Title: 核心对象/函数说明
Describe: 关键类、数据结构和核心函数的职责与接口
------------------------------------------------------------
[Example]
Task 数据类

[Description]
<!-- Section-ID: S03_CORE_OBJECTS -->
**Task**（行 78-86）是任务系统的核心数据结构，使用 `@dataclass` 装饰器。

**字段**：
- `id: str`：任务唯一标识，格式 `task_{timestamp}_{random}`
- `subject: str`：任务标题
- `description: str`：任务描述
- `status: str`：状态（pending/in_progress/completed）
- `owner: str | None`：所有者（agent 或队友名）
- `blockedBy: list[str]`：依赖的任务 ID 列表
- `worktree: str | None`：绑定的工作树名称

**持久化**：每个任务存储为 `.tasks/{task_id}.json` 文件。

[UML]
None

[Detail]
**关键操作**：
- `create_task()`（行 93-102）：创建并持久化任务
- `save_task()`（行 105-106）：序列化到 JSON 文件
- `load_task()`（行 109-110）：从 JSON 文件反序列化
- `list_tasks()`（行 113-115）：列出所有任务
- `claim_task()`（行 134-152）：认领任务，检查依赖和所有权
- `complete_task()`（行 155-167）：完成任务，返回被解锁的任务

**依赖检查**（`can_start()`，行 122-131）：
- 所有依赖任务必须存在
- 所有依赖任务必须已完成
- 用于支持任务图（DAG）调度

------------------------------------------------------------
[Example]
MessageBus 类

[Description]
<!-- Section-ID: S03_CORE_OBJECTS -->
**MessageBus**（行 479-498）实现团队通信，使用 JSONL 格式的邮箱文件。

**核心方法**：
- `send(from_agent, to_agent, content, msg_type, metadata)`：发送消息
- `read_inbox(agent)`：读取并清空邮箱

**消息格式**：
```json
{
  "from": "agent_name",
  "to": "target_agent",
  "content": "message content",
  "type": "message|shutdown_request|plan_approval_request|...",
  "ts": 1234567890.123,
  "metadata": {"request_id": "req_123456"}
}
```

[UML]
None

[Detail]
**设计决策**：
- **追加写入**：消息追加到 `.mailboxes/{agent}.jsonl`，避免锁竞争
- **读取即删除**：`read_inbox()` 读取后立即删除文件，确保消息只被处理一次
- **协议路由**：`consume_lead_inbox()` 支持自动路由协议响应

**用途**：
- 主导（lead）与队友通信
- 计划审批请求/响应
- 关闭请求/响应
- 任务结果报告

------------------------------------------------------------
[Example]
CronJob 数据类

[Description]
<!-- Section-ID: S03_CORE_OBJECTS -->
**CronJob**（行 1282-1289）是定时任务的数据结构。

**字段**：
- `id: str`：任务 ID，格式 `cron_{random}`
- `cron: str`：5 字段 cron 表达式（分 时 日 月 周）
- `prompt: str`：触发时注入的提示
- `recurring: bool`：是否重复执行
- `durable: bool`：是否持久化到 `.scheduled_tasks.json`

**关键函数**：
- `cron_matches()`（行 1312-1331）：检查当前时间是否匹配 cron 表达式
- `validate_cron()`（行 1366-1376）：验证 cron 表达式格式
- `schedule_job()`（行 1396-1409）：创建定时任务

[UML]
None

[Detail]
**cron 表达式支持**：
- 通配符：`*`
- 步长：`*/5`（每5分钟）
- 范围：`1-5`
- 列表：`1,3,5`

**调度器线程**（`cron_scheduler_loop()`，行 1422-1438）：
- 每秒检查一次
- 使用 `_last_fired` 字典防止同一分钟内重复触发
- 非重复任务触发后自动删除

**持久化**：
- `save_durable_jobs()`（行 1379-1381）：保存到 `.scheduled_tasks.json`
- `load_durable_jobs()`（行 1384-1393）：启动时加载

------------------------------------------------------------
[Example]
MCPClient 类

[Description]
<!-- Section-ID: S03_CORE_OBJECTS -->
**MCPClient**（行 1480-1501）是 MCP（Model Context Protocol）客户端的模拟实现。

**属性**：
- `name: str`：服务器名称
- `tools: list[dict]`：发现的工具定义
- `_handlers: dict[str, callable]`：工具处理器映射

**方法**：
- `register(tool_defs, handlers)`：注册工具定义和处理器
- `call_tool(tool_name, args)`：调用工具

[UML]
None

[Detail]
**MCP 工具命名规范**：
- 格式：`mcp__{server}__{tool}`
- 服务器名和工具名中的非字母数字字符替换为下划线
- 例如：`mcp__docs__search`、`mcp__deploy__trigger`

**模拟服务器**（行 1513-1556）：
- `docs`：文档搜索和版本查询（只读）
- `deploy`：部署触发和状态查询（destructive + readOnly）

**工具池组装**（`assemble_tool_pool()`，行 1574-1590）：
- 合并内置工具和所有 MCP 工具
- MCP 工具使用 lambda 闭包绑定客户端和工具名
