Title: 顶层结构
Describe: 文件的导入、常量、全局变量和主要分区
------------------------------------------------------------
[Example]
导入依赖与环境配置

[Description]
<!-- Section-ID: S02_TOP_LEVEL_STRUCTURE -->
文件的导入分为四个层次：

1. **标准库**：os、subprocess、json、time、random、threading、re、pathlib、datetime、dataclasses
2. **可选依赖**：readline（用于终端行编辑）
3. **核心依赖**：anthropic（Claude API）、dotenv（环境变量）
4. **启动配置**：加载 .env，配置客户端，设置工作目录

**关键常量**：
- `WORKDIR`：当前工作目录，所有文件路径的基准
- `MODEL` / `PRIMARY_MODEL` / `FALLBACK_MODEL`：模型配置
- `SKILLS_DIR`、`TRANSCRIPT_DIR`、`TOOL_RESULTS_DIR`：数据目录

[UML]
None

[Detail]
**导入时副作用**：
- `readline.parse_and_bind('set bind-tty-special-chars off')`：禁用 readline 的 tty 特殊字符绑定
- `load_dotenv(override=True)`：从 .env 文件加载环境变量
- `os.environ.pop("ANTHROPIC_AUTH_TOKEN", None)`：当设置了 BASE_URL 时清除 AUTH_TOKEN

**启动常量**（行 43-54）：
- `DEFAULT_MAX_TOKENS = 8000`
- `ESCALATED_MAX_TOKENS = 16000`
- `MAX_RETRIES = 3`
- `MAX_CONSECUTIVE_529 = 2`
- `MAX_RECOVERY_RETRIES = 2`
- `BASE_DELAY_MS = 500`
- `CONTEXT_LIMIT = 50000`
- `KEEP_RECENT_TOOL_RESULTS = 3`
- `PERSIST_THRESHOLD = 30000`

------------------------------------------------------------
[Example]
文件逻辑分区

[Description]
<!-- Section-ID: S02_TOP_LEVEL_STRUCTURE -->
文件按功能划分为 17 个明显的分区，每个分区以 `# ── 分区名 ──` 注释开头：

1. **Task System**（行 70-168）：任务数据结构和 CRUD 操作
2. **Worktree System**（行 170-281）：Git 工作树管理
3. **Skill Loading**（行 283-339）：技能扫描和加载
4. **Prompt Assembly**（行 342-373）：系统提示构建
5. **Basic Tools**（行 376-468）：文件和 shell 工具
6. **MessageBus**（行 471-503）：团队通信
7. **Protocol State**（行 504-546）：协议请求管理
8. **Autonomous Agent**（行 548-600）：空闲轮询和自动认领
9. **Teammate Thread**（行 602-822）：队友线程管理
10. **Lead Protocol Tools**（行 824-852）：主导协议工具
11. **Hooks + Permission Pipeline**（行 855-940）：钩子系统
12. **Subagent Tool**（行 942-1033）：子代理
13. **Context Compaction**（行 1036-1148）：上下文压缩
14. **Error Recovery**（行 1151-1202）：错误恢复
15. **Background Tasks**（行 1204-1272）：后台任务
16. **Cron Scheduler**（行 1275-1473）：定时任务
17. **MCP System**（行 1476-1664）：MCP 集成

[UML]
None

[Detail]
**入口点**（行 2034-2069）：
- `if __name__ == "__main__":` 块启动 CLI 交互循环
- 启动 cron_autorun_loop 后台线程
- 主循环：读取用户输入 → 触发钩子 → 调用 agent_loop → 处理邮箱

**全局状态**：
- `client`：Anthropic API 客户端
- `SKILL_REGISTRY`：技能注册表
- `HOOKS`：钩子回调列表
- `mcp_clients`：MCP 客户端映射
- `active_teammates`：活跃队友
- `pending_requests`：待处理协议请求
- `scheduled_jobs`：定时任务
- `background_tasks`：后台任务
- `background_results`：后台结果
