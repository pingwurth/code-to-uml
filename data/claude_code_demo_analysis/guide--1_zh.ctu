Title: 开发上手指南
Describe: 阅读顺序、运行调试步骤和扩展点
------------------------------------------------------------
[Example]
代码阅读顺序

[Description]
<!-- Section-ID: S10_ONBOARDING_GUIDE -->
**推荐阅读顺序**：

1. **入口点**（行 2034-2069）：了解系统如何启动
2. **主循环**（行 1901-2004）：理解核心执行流程
3. **任务系统**（行 70-168）：理解数据结构和状态管理
4. **消息总线**（行 471-503）：理解团队通信机制
5. **钩子系统**（行 855-940）：理解扩展点
6. **上下文压缩**（行 1036-1148）：理解上下文管理
7. **错误恢复**（行 1151-1202）：理解容错机制
8. **队友线程**（行 602-822）：理解团队协作
9. **定时任务**（行 1275-1473）：理解调度机制
10. **MCP 系统**（行 1476-1664）：理解工具集成

**阅读策略**：
- 先理解整体架构，再深入细节
- 先理解数据流，再理解控制流
- 先理解主循环，再理解子系统

[UML]
None

[Detail]
**快速上手路径**：

**第一步：运行系统**
```bash
# 设置环境变量
export ANTHROPIC_API_KEY="your-key"
export MODEL_ID="claude-3-sonnet-20240229"

# 运行
python claude_code_demo.py
```

**第二步：体验基本功能**
1. 输入任务描述，观察 LLM 响应
2. 使用 `create_task` 创建任务
3. 使用 `list_tasks` 查看任务
4. 使用 `claim_task` 和 `complete_task` 管理任务

**第三步：探索高级功能**
1. 使用 `spawn_teammate` 创建队友
2. 使用 `schedule_cron` 创建定时任务
3. 使用 `connect_mcp` 连接 MCP 服务器
4. 观察日志输出，理解系统行为

**调试技巧**：
- 查看 `.tasks/` 目录了解任务状态
- 查看 `.mailboxes/` 目录了解消息
- 查看 `.worktrees/events.jsonl` 了解工作树事件
- 查看 `.scheduled_tasks.json` 了解定时任务

------------------------------------------------------------
[Example]
扩展点指南

[Description]
<!-- Section-ID: S10_ONBOARDING_GUIDE -->
**可扩展的组件**：

1. **添加新工具**：
   - 在 `BUILTIN_TOOLS` 添加工具定义
   - 在 `BUILTIN_HANDLERS` 添加处理器
   - 在 `SUB_TOOLS` 和 `SUB_HANDLERS` 为子代理添加

2. **添加新钩子**：
   - 使用 `register_hook()` 注册
   - 实现回调函数，返回 `None` 继续或字符串停止

3. **添加新协议**：
   - 在 `ProtocolState.type` 添加新类型
   - 在 `match_response()` 添加匹配逻辑
   - 在 `consume_lead_inbox()` 添加路由逻辑

4. **添加新 MCP 服务器**：
   - 创建 `MCPClient` 实例
   - 注册工具定义和处理器
   - 添加到 `MOCK_SERVERS` 字典

[UML]
None

[Detail]
**工具扩展示例**：
```python
# 添加新工具定义
{"name": "search_code", "description": "Search codebase.",
 "input_schema": {"type": "object",
                  "properties": {"query": {"type": "string"}},
                  "required": ["query"]}}

# 添加处理器
"search_code": lambda query: run_search(query)
```

**钩子扩展示例**：
```python
# 注册钩子
def my_hook(block):
    print(f"Tool called: {block.name}")
    return None  # 继续执行

register_hook("PreToolUse", my_hook)
```

**协议扩展示例**：
```python
# 添加新协议类型
def match_response(response_type, request_id, approve):
    state = pending_requests.get(request_id)
    if not state:
        return
    if state.type == "my_protocol" and response_type != "my_protocol_response":
        return
    state.status = "approved" if approve else "rejected"
```

**注意事项**：
- 工具名称不能包含特殊字符
- 钩子回调必须返回 None 或字符串
- 协议响应必须匹配 request_id 和 type

------------------------------------------------------------
[Example]
风险与改进建议

[Description]
<!-- Section-ID: S11_RISKS_AND_IMPROVEMENTS -->
**已知风险**：

1. **并发安全**：
   - 任务系统没有显式锁
   - 文件操作可能竞争
   - 建议：使用文件锁或数据库

2. **错误恢复**：
   - 消息丢失无法恢复
   - 没有事务支持
   - 建议：添加消息队列和事务日志

3. **性能瓶颈**：
   - 文件 I/O 是瓶颈
   - 上下文压缩延迟
   - 建议：使用内存缓存和异步 I/O

4. **安全风险**：
   - 权限检查不完整
   - 没有沙箱隔离
   - 建议：添加更严格的权限控制

5. **可扩展性**：
   - 单进程架构
   - 没有分布式支持
   - 建议：使用消息队列和微服务

[UML]
None

[Detail]
**具体改进建议**：

**短期改进**：
1. 添加文件锁（`fcntl.flock`）
2. 添加消息重试机制
3. 添加配置文件支持
4. 添加日志级别控制

**中期改进**：
1. 使用 SQLite 替代 JSON 文件
2. 添加异步 I/O 支持
3. 添加单元测试
4. 添加性能监控

**长期改进**：
1. 使用消息队列（Redis/RabbitMQ）
2. 添加分布式支持
3. 添加 Web UI
4. 添加插件系统

**代码质量改进**：
1. 添加类型注解
2. 添加文档字符串
3. 重构长函数
4. 提取配置常量

------------------------------------------------------------
[Example]
Q&A / 复盘问答清单

[Description]
<!-- Section-ID: S12_REVIEWER_QUESTIONS -->
**学习复盘问题**：

**Q1：为什么选择文件系统作为状态存储？**
A：教学优先，可检查性和可调试性比性能更重要。文件系统是通用的，不需要额外依赖。

**Q2：消息总线为什么使用 JSONL 格式？**
A：JSONL 支持追加写入，避免锁竞争；每行一个 JSON，易于解析；人类可读，便于调试。

**Q3：钩子系统如何实现权限控制？**
A：PreToolUse 钩子在工具执行前拦截，检查命令是否在 deny list 或需要用户确认。返回 None 允许执行，返回字符串阻止执行。

**Q4：上下文压缩如何防止信息丢失？**
A：使用多层策略，从轻量级到重量级；压缩前保存转录文件；保留最近的消息；使用 LLM 生成摘要保留关键信息。

**Q5：协议状态匹配如何防止混乱？**
A：使用随机生成的 request_id 匹配请求和响应；响应类型必须匹配请求类型；一个响应不能批准另一个请求。

[UML]
None

[Detail]
**更多问题**：

**Q6：队友线程如何实现工作树隔离？**
A：当任务绑定工作树时，所有文件工具透明地在隔离目录中运行。`_wt_cwd()` 返回工作树路径，`_run_bash()`、`_run_read()`、`_run_write()` 使用工作树路径。

**Q7：后台任务如何避免阻塞主循环？**
A：检测到慢操作时，创建后台任务记录，启动工作线程，返回占位符结果。工作线程完成后，结果通过 task_notification 注入。

**Q8：定时任务如何防止重复触发？**
A：使用 `_last_fired` 字典记录上次触发时间，格式为 `"{job_id}": "YYYY-MM-DD HH:MM"`。同一分钟内不会重复触发。

**Q9：MCP 系统如何实现动态工具发现？**
A：MCPClient 注册工具定义和处理器，`assemble_tool_pool()` 合并内置工具和 MCP 工具。MCP 工具使用 lambda 闭包绑定客户端和工具名。

**Q10：错误恢复如何实现模型降级？**
A：`RecoveryState` 跟踪连续 529 错误次数，达到阈值（2次）且有备用模型时，切换到 `FALLBACK_MODEL`。降级后重置计数器。

------------------------------------------------------------
[Example]
维护者速查表

[Description]
<!-- Section-ID: S13_MAINTAINER_REFERENCE -->
**核心函数和类速查**：

| 名称 | 类型 | 位置 | 用途 |
|---|---|---|---|
| `Task` | 数据类 | 行 78-86 | 任务数据结构 |
| `MessageBus` | 类 | 行 479-498 | 团队通信 |
| `ProtocolState` | 数据类 | 行 506-514 | 协议状态 |
| `CronJob` | 数据类 | 行 1282-1289 | 定时任务 |
| `MCPClient` | 类 | 行 1480-1501 | MCP 客户端 |
| `RecoveryState` | 类 | 行 1153-1159 | 错误恢复状态 |
| `agent_loop()` | 函数 | 行 1901-2004 | 主代理循环 |
| `spawn_teammate_thread()` | 函数 | 行 605-809 | 创建队友线程 |
| `spawn_subagent()` | 函数 | 行 1004-1033 | 创建子代理 |
| `permission_hook()` | 函数 | 行 879-904 | 权限检查 |
| `with_retry()` | 函数 | 行 1167-1194 | 重试机制 |
| `prepare_context()` | 函数 | 行 1861-1868 | 上下文准备 |
| `compact_history()` | 函数 | 行 1133-1137 | 上下文压缩 |
| `assemble_tool_pool()` | 函数 | 行 1574-1590 | 工具池组装 |
| `assemble_system_prompt()` | 函数 | 行 359-373 | 系统提示构建 |

[UML]
None

[Detail]
**常量速查**：

| 名称 | 值 | 位置 | 用途 |
|---|---|---|---|
| `DEFAULT_MAX_TOKENS` | 8000 | 行 43 | 默认 token 限制 |
| `ESCALATED_MAX_TOKENS` | 16000 | 行 44 | 升级后 token 限制 |
| `MAX_RETRIES` | 3 | 行 45 | 最大重试次数 |
| `MAX_CONSECUTIVE_529` | 2 | 行 46 | 连续 529 错误阈值 |
| `MAX_RECOVERY_RETRIES` | 2 | 行 47 | 最大恢复尝试次数 |
| `BASE_DELAY_MS` | 500 | 行 48 | 基础重试延迟 |
| `CONTEXT_LIMIT` | 50000 | 行 49 | 上下文大小限制 |
| `KEEP_RECENT_TOOL_RESULTS` | 3 | 行 50 | 保留的最近工具结果数 |
| `PERSIST_THRESHOLD` | 30000 | 行 51 | 工具结果持久化阈值 |
| `IDLE_POLL_INTERVAL` | 5 | 行 551 | 空闲轮询间隔（秒） |
| `IDLE_TIMEOUT` | 60 | 行 552 | 空闲超时（秒） |

**目录结构速查**：

| 目录 | 用途 |
|---|---|
| `.tasks/` | 任务存储 |
| `.mailboxes/` | 邮箱存储 |
| `.worktrees/` | 工作树存储 |
| `.memory/` | 记忆存储 |
| `.transcripts/` | 转录文件 |
| `.task_outputs/tool-results/` | 工具结果持久化 |
| `.scheduled_tasks.json` | 定时任务持久化 |
| `skills/` | 技能定义 |
