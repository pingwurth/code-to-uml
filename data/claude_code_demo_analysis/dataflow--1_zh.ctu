Title: 数据流/状态流图
Describe: 数据移动、状态所有权和生命周期
------------------------------------------------------------
[Example]
任务状态机

[Description]
<!-- Section-ID: S07_DATA_OR_STATE_FLOW -->
任务系统实现了简单的状态机，支持三种状态和两个转换。

**状态**：
- `pending`：初始状态，等待认领
- `in_progress`：已认领，正在执行
- `completed`：已完成

**转换条件**：
- pending → in_progress：任务被认领（`claim_task()`）
- in_progress → completed：任务被完成（`complete_task()`）

**依赖解锁**：
- 当任务完成时，检查是否有其他任务被解锁
- 被解锁的任务可以被认领

[UML]
@startuml
[*] --> pending : create_task()

pending --> in_progress : claim_task()
note right of pending : 检查依赖\n检查所有权

in_progress --> completed : complete_task()
note left of in_progress : 解锁依赖任务

completed --> [*]
@enduml

[Detail]
**状态存储**：
- 每个任务存储为 `.tasks/{task_id}.json`
- 状态字段：`status: "pending" | "in_progress" | "completed"`

**依赖检查**（`can_start()`，行 122-131）：
- 所有 `blockedBy` 中的任务必须存在
- 所有 `blockedBy` 中的任务必须已完成
- 用于支持任务图（DAG）调度

**解锁逻辑**（`complete_task()`，行 155-167）：
- 遍历所有 pending 任务
- 检查 `can_start()` 是否返回 true
- 返回被解锁的任务列表

**并发安全**：
- 文件系统操作是原子的（单次写入）
- 没有显式锁，依赖文件系统的原子性
- 可能在高并发场景下出现竞争条件

------------------------------------------------------------
[Example]
消息生命周期

[Description]
<!-- Section-ID: S07_DATA_OR_STATE_FLOW -->
消息在系统中的生命周期，从创建到消费。

**生命周期**：
1. **创建**：`MessageBus.send()` 创建消息对象
2. **持久化**：追加到 `.mailboxes/{agent}.jsonl`
3. **读取**：`read_inbox()` 读取整个文件
4. **消费**：读取后立即删除文件
5. **处理**：消息被路由到处理器

**消息类型**：
- `message`：普通消息
- `result`：任务结果
- `shutdown_request/response`：关闭协议
- `plan_approval_request/response`：计划审批协议

[UML]
@startuml
[*] --> 创建 : MessageBus.send()

创建 --> 持久化 : 追加到 .jsonl 文件
持久化 --> 读取 : read_inbox()
读取 --> 消费 : 删除文件

消费 --> 路由 : consume_lead_inbox()

路由 --> 普通消息 : type == "message"
路由 --> 协议响应 : type ends with "_response"

普通消息 --> [*]
协议响应 --> 匹配 : match_response()
匹配 --> [*]
@enduml

[Detail]
**文件格式**：
- JSONL（每行一个 JSON 对象）
- 追加写入，避免锁竞争
- 读取即删除，确保消息只被处理一次

**协议路由**（`consume_lead_inbox()`，行 537-546）：
- 检查 `metadata.request_id`
- 检查 `type` 是否以 `_response` 结尾
- 调用 `match_response()` 匹配请求

**消息队列特性**：
- 无持久化队列，依赖文件系统
- 无重试机制，消息丢失后无法恢复
- 无优先级，按文件顺序处理

------------------------------------------------------------
[Example]
定时任务生命周期

[Description]
<!-- Section-ID: S07_DATA_OR_STATE_FLOW -->
定时任务从创建到执行的完整生命周期。

**生命周期**：
1. **创建**：`schedule_job()` 创建 CronJob
2. **持久化**：保存到 `.scheduled_tasks.json`（如果 durable）
3. **调度**：`cron_scheduler_loop()` 每秒检查
4. **触发**：匹配 cron 表达式，加入队列
5. **消费**：`consume_cron_queue()` 取出并清空
6. **注入**：作为 `[Scheduled]` 消息注入对话

**状态**：
- 活跃：在 `scheduled_jobs` 字典中
- 触发：在 `cron_queue` 队列中
- 完成：从字典中移除（非重复任务）

[UML]
@startuml
[*] --> 创建 : schedule_job()

创建 --> 持久化 : save_durable_jobs()
持久化 --> 调度 : cron_scheduler_loop()

调度 --> 触发 : cron_matches()

触发 --> 消费 : consume_cron_queue()
消费 --> 注入 : agent_loop()

注入 --> [*]

调度 --> 移除 : 非重复任务
移除 --> [*]
@enduml

[Detail]
**防重复机制**：
- `_last_fired: dict[str, str]`：记录上次触发时间
- 格式：`"{job_id}": "YYYY-MM-DD HH:MM"`
- 同一分钟内不会重复触发

**持久化**：
- 只有 `durable=true` 的任务才会保存到文件
- 启动时调用 `load_durable_jobs()` 加载
- 任务变更后调用 `save_durable_jobs()` 保存

**调度器线程**（`cron_scheduler_loop()`，行 1422-1438）：
- 独立线程，每秒检查一次
- 使用 `cron_lock` 保护共享状态
- 非重复任务触发后自动删除
