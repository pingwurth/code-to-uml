Title: 核心流程图
Describe: 主执行路径、错误处理路径和后台任务路径
------------------------------------------------------------
[Example]
主代理循环流程

[Description]
<!-- Section-ID: S05_CORE_FLOW -->
**agent_loop()**（行 1901-2004）是系统的核心循环，处理用户输入并协调所有组件。

**主路径**：
1. 消费定时任务 → 注入后台通知 → 提醒更新 todo
2. 准备上下文 → 更新上下文 → 组装工具池
3. 调用 LLM → 检查停止原因
4. 处理工具调用 → 构建用户内容 → 循环

**关键分支**：
- `max_tokens`：升级 token 限制或发送继续提示
- `compact` 工具：压缩历史并继续
- `should_run_background()`：后台执行慢操作

[UML]
@startuml
start
:消费定时任务队列;
:注入后台任务通知;
:提醒更新 todo (每3轮);

if (上下文超过限制?) then (是)
  :压缩历史;
endif

:准备上下文;
:更新上下文;
:组装工具池;

repeat
  :调用 LLM;
  
  if (prompt too long?) then (是)
    if (已尝试响应式压缩?) then (否)
      :响应式压缩;
      ->返回循环开始;
    else (是)
      :记录错误;
      stop
    endif
  endif
  
  if (stop_reason == max_tokens?) then (是)
    if (未升级?) then (是)
      :升级 token 限制;
      ->返回循环开始;
    else (否)
      if (恢复次数 < 限制?) then (是)
        :发送继续提示;
        ->返回循环开始;
      else (否)
        stop
      endif
    endif
  endif
  
  if (有工具调用?) then (是)
    :处理工具调用;
    :构建用户内容;
  else (否)
    :触发 Stop 钩子;
    stop
  endif
repeat while (继续循环?)
@enduml

[Detail]
**错误恢复策略**：
1. **429 错误**：指数退避重试，最多 3 次
2. **529 错误**：连续 2 次后切换到备用模型
3. **prompt too long**：响应式压缩，保留最近 5 条消息
4. **max_tokens**：升级到 16000 tokens，或发送继续提示

**上下文准备流程**（`prepare_context()`，行 1861-1868）：
1. `tool_result_budget()`：限制工具结果总大小
2. `snip_compact()`：裁剪旧消息
3. `micro_compact()`：压缩旧工具结果
4. `compact_history()`：如果仍然过大，完全压缩

------------------------------------------------------------
[Example]
队友线程执行流程

[Description]
<!-- Section-ID: S05_CORE_FLOW -->
**Teammate Thread**（行 634-809）在独立线程中运行，支持自主任务认领和协议交互。

**执行流程**：
1. 初始化：设置工具、处理器、身份提示
2. 主循环：检查邮箱 → 处理协议 → 调用 LLM → 执行工具
3. 空闲轮询：等待消息或自动认领任务
4. 退出：发送结果给主导

[UML]
@startuml
start
:初始化工具和处理器;
:添加身份提示;

repeat
  :检查邮箱;
  
  if (收到关闭请求?) then (是)
    :发送关闭响应;
    stop
  endif
  
  if (等待计划审批?) then (是)
    :等待审批;
    ->返回循环开始;
  endif
  
  if (有新消息?) then (是)
    :添加到消息列表;
  endif
  
  :调用 LLM;
  
  if (有工具调用?) then (是)
    :执行工具;
    
    if (submit_plan?) then (是)
      :发送计划审批请求;
      :设置等待状态;
      ->退出循环;
    endif
  else (否)
    ->退出循环;
  endif
repeat while (继续?)

:发送结果给主导;
:从活跃队友列表移除;
stop
@enduml

[Detail]
**工作树隔离**：
- 当任务绑定工作树时，所有文件工具透明地在隔离目录中运行
- `_wt_cwd()` 返回工作树路径
- `_run_bash()`、`_run_read()`、`_run_write()` 使用工作树路径

**协议交互**：
- `submit_plan`：提交计划，等待主导审批
- `plan_approval_response`：接收审批结果
- `shutdown_request`：接收关闭请求

**空闲轮询**（`idle_poll()`，行 566-600）：
- 每 5 秒检查一次
- 优先处理邮箱消息
- 其次自动认领未认领的任务
- 超时 60 秒后退出

------------------------------------------------------------
[Example]
后台任务执行流程

[Description]
<!-- Section-ID: S05_CORE_FLOW -->
**Background Tasks**（行 1204-1272）处理慢操作，避免阻塞主循环。

**触发条件**：
- `run_in_background: true`（显式请求）
- `is_slow_operation()`：检测到慢操作关键词

**执行流程**：
1. 创建后台任务记录
2. 启动工作线程
3. 返回占位符工具结果
4. 工作线程完成后，结果通过 `task_notification` 注入

[UML]
@startuml
start
:检测是否慢操作;

if (需要后台执行?) then (是)
  :创建后台任务记录;
  :启动工作线程;
  :返回占位符结果;
  
  fork
    :工作线程执行;
    :记录结果;
    :标记完成;
  fork again
    :主循环继续;
    
    repeat
      :收集完成的任务;
      
      if (有完成的任务?) then (是)
        :注入 task_notification;
      endif
      
      :继续正常处理;
    repeat while (主循环继续?)
  end fork
else (否)
  :同步执行;
endif
stop
@enduml

[Detail]
**慢操作检测**（`is_slow_operation()`，行 1214-1221）：
- 检查命令中的关键词：install、build、test、deploy、compile、docker build、pip install、npm install、cargo build、pytest、make

**后台任务状态**：
- `running`：正在执行
- `completed`：已完成，结果可用

**结果注入**（`collect_background_results()`，行 1255-1272）：
- 格式：`<task_notification>` XML 标签
- 包含：task_id、status、command、summary（前 200 字符）
