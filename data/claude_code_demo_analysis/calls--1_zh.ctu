Title: 调用关系图
Describe: 跨组件的调用链和交互序列
------------------------------------------------------------
[Example]
工具调用链

[Description]
<!-- Section-ID: S06_CALL_RELATIONSHIPS -->
展示从 LLM 响应到工具执行的完整调用链。

**调用链**：
1. `agent_loop()` 接收 LLM 响应
2. 遍历 `response.content` 中的 `tool_use` 块
3. 检查是否为 `compact` 工具
4. 触发 `PreToolUse` 钩子
5. 检查是否需要后台执行
6. 调用 `call_tool_handler()` 执行工具
7. 触发 `PostToolUse` 钩子
8. 构建 `tool_result` 并追加到结果列表

[UML]
@startuml
participant "agent_loop" as Loop
participant "PreToolUse Hooks" as Pre
participant "Background Check" as BG
participant "call_tool_handler" as Handler
participant "PostToolUse Hooks" as Post
participant "Tool Function" as Tool

Loop -> Loop: 遍历 tool_use 块
Loop -> Loop: 检查 compact 工具

alt 是 compact
  Loop -> Loop: compact_history()
  Loop -> Loop: break
else 其他工具
  Loop -> Pre: trigger_hooks()
  
  alt 被拒绝
    Pre --> Loop: 拒绝原因
  else 允许
    Loop -> BG: should_run_background()
    
    alt 需要后台
      BG --> Loop: bg_id
      Loop -> Loop: 返回占位符
    else 同步执行
      Loop -> Handler: call_tool_handler()
      Handler -> Tool: 调用工具函数
      Tool --> Handler: 结果
      Handler --> Loop: 输出
      Loop -> Post: trigger_hooks()
    end
  end
  
  Loop -> Loop: 构建 tool_result
end
@enduml

[Detail]
**钩子执行顺序**：
1. `permission_hook`：检查权限（deny list、destructive 命令、MCP deploy）
2. `log_hook`：记录工具调用
3. `large_output_hook`：检查大输出

**权限检查**（`permission_hook()`，行 879-904）：
- `DENY_LIST`：rm -rf /、sudo、shutdown、reboot、mkfs、dd if=
- `DESTRUCTIVE`：rm 、> /etc/、chmod 777
- MCP deploy 工具需要用户确认

------------------------------------------------------------
[Example]
协议交互序列

[Description]
<!-- Section-ID: S06_CALL_RELATIONSHIPS -->
展示计划审批协议的完整交互序列。

**参与者**：
- Lead（主导）
- Teammate（队友）
- ProtocolState（协议状态）
- MessageBus（消息总线）

[UML]
@startuml
participant "Lead" as Lead
participant "Teammate" as TM
participant "ProtocolState" as PS
participant "MessageBus" as Bus

Lead -> TM: request_plan(task)
TM -> TM: 生成计划
TM -> PS: 创建 ProtocolState
TM -> Bus: send(plan_approval_request)
Bus --> Lead: 投递到 lead 邮箱

Lead -> Lead: consume_lead_inbox()
Lead -> Lead: 匹配 request_id
Lead -> PS: match_response()

alt 批准
  Lead -> Bus: send(plan_approval_response, approve=true)
  Bus --> TM: 投递到 teammate 邮箱
  TM -> TM: 继续执行
else 拒绝
  Lead -> Bus: send(plan_approval_response, approve=false)
  Bus --> TM: 投递到 teammate 邮箱
  TM -> TM: 接收反馈
end
@enduml

[Detail]
**协议状态管理**：
- `pending_requests: dict[str, ProtocolState]`：待处理请求
- `new_request_id()`：生成随机请求 ID
- `match_response()`：匹配响应到请求

**消息类型**：
- `plan_approval_request`：计划审批请求
- `plan_approval_response`：计划审批响应
- `shutdown_request`：关闭请求
- `shutdown_response`：关闭响应

**安全边界**：
- 协议响应必须匹配 request_id
- 响应类型必须匹配请求类型
- 防止一个响应意外批准另一个请求

------------------------------------------------------------
[Example]
上下文压缩调用链

[Description]
<!-- Section-ID: S06_CALL_RELATIONSHIPS -->
展示上下文压缩的多层调用链。

**压缩层次**：
1. `tool_result_budget()`：限制工具结果总大小
2. `snip_compact()`：裁剪旧消息
3. `micro_compact()`：压缩旧工具结果
4. `compact_history()`：完全压缩

[UML]
@startuml
participant "prepare_context" as PC
participant "tool_result_budget" as TRB
participant "snip_compact" as SC
participant "micro_compact" as MC
participant "compact_history" as CH
participant "summarize_history" as SH
participant "write_transcript" as WT

PC -> TRB: 限制工具结果大小
TRB -> TRB: 检查总字节数

alt 超过 200KB
  TRB -> TRB: persist_large_output()
  TRB -> TRB: 替换为预览
end

PC -> SC: 裁剪旧消息

alt 超过 50 条
  SC -> SC: 保留前 3 条 + 后 47 条
  SC -> SC: 插入 [snipped N messages]
end

PC -> MC: 压缩旧工具结果
MC -> MC: 保留最近 3 个工具结果
MC -> MC: 替换旧结果为占位符

opt 仍然超过限制
  PC -> CH: 完全压缩
  CH -> WT: 写入转录文件
  CH -> SH: 调用 LLM 生成摘要
  SH --> CH: 摘要文本
  CH --> PC: 压缩后的消息
end
@enduml

[Detail]
**预算机制**：
- `PERSIST_THRESHOLD = 30000`：超过 30KB 的工具结果持久化到文件
- `tool_result_budget()`：限制单条消息的工具结果总大小为 200KB
- `KEEP_RECENT_TOOL_RESULTS = 3`：保留最近 3 个工具结果

**压缩策略**：
- `snip_compact()`：保留前 3 条和后 47 条消息
- `micro_compact()`：只压缩超过 120 字符的旧工具结果
- `compact_history()`：完全压缩，只保留摘要

**持久化输出**：
- 存储在 `.task_outputs/tool-results/{tool_use_id}.txt`
- 返回预览（前 2000 字符）+ 文件路径
