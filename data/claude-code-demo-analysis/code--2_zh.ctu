Title: 权限钩子与上下文压缩
Describe: 权限检查钩子和上下文压缩策略的代码实现。
------------------------------------------------------------

[Example]
权限检查钩子

[Description]
<!-- Section-ID: S08_CODE_SNIPPETS -->
**permission_hook 函数（第 879-904 行）：**

```python
def permission_hook(block):
    # 1. Bash 命令检查
    if block.name == "bash":
        command = block.input.get("command", "")
        for pattern in DENY_LIST:
            if pattern in command:
                return f"Permission denied: '{pattern}' is on the deny list"
        if any(token in command for token in DESTRUCTIVE):
            print(f"\n\033[33m[permission] destructive command\033[0m")
            print(f"  {command}")
            choice = input("  Allow? [y/N] ").strip().lower()
            if choice not in ("y", "yes"):
                return "Permission denied by user"
    
    # 2. 文件路径检查
    if block.name in ("write_file", "edit_file"):
        path = block.input.get("path", "")
        try:
            safe_path(path)
        except Exception:
            return f"Permission denied: path escapes workspace: {path}"
    
    # 3. MCP 部署工具检查
    if block.name.startswith("mcp__") and "deploy" in block.name:
        print(f"\n\033[33m[permission] MCP destructive-looking tool: {block.name}\033[0m")
        choice = input("  Allow? [y/N] ").strip().lower()
        if choice not in ("y", "yes"):
            return "Permission denied by user"
    
    return None
```

**拒绝列表（第 875-876 行）：**

```python
DENY_LIST = ["rm -rf /", "sudo", "shutdown", "reboot", "mkfs", "dd if="]
DESTRUCTIVE = ["rm ", "> /etc/", "chmod 777"]
```

[UML]
None

[Detail]
**权限检查层次：**

1. **绝对拒绝**：`DENY_LIST` 中的模式直接拒绝
2. **用户确认**：`DESTRUCTIVE` 中的模式需要用户确认
3. **路径检查**：文件操作检查路径是否逃逸工作区
4. **MCP 检查**：部署相关的 MCP 工具需要确认

**设计决策：**

- 钩子在工具执行前调用（`PreToolUse`）
- 返回 `None` 表示允许，返回字符串表示拒绝
- 用户交互通过 `input()` 实现

**上下文压缩策略（第 1036-1148 行）：**

```python
def prepare_context(messages: list) -> list:
    # 1. 工具结果预算
    messages[:] = tool_result_budget(messages)
    # 2. 截断旧消息
    messages[:] = snip_compact(messages)
    # 3. 压缩旧工具结果
    messages[:] = micro_compact(messages)
    # 4. LLM 摘要（如果仍然太大）
    if estimate_size(messages) > CONTEXT_LIMIT:
        messages[:] = compact_history(messages)
    return messages
```

**压缩策略详解：**

| 策略 | 函数 | 触发条件 | 处理方式 |
|------|------|----------|----------|
| 工具结果预算 | `tool_result_budget()` | 结果 > 200KB | 持久化大型结果 |
| 消息截断 | `snip_compact()` | 消息 > 50 条 | 保留前 3 + 后 47 条 |
| 工具结果压缩 | `micro_compact()` | 旧结果 > 3 条 | 压缩为占位符 |
| LLM 摘要 | `compact_history()` | 总大小 > 50KB | 生成摘要 |
