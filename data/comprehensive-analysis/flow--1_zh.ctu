Title: 核心流程图
Describe: 主循环每轮注入外部事件、压缩上下文、调用模型、执行工具，并把结果反馈给模型。
------------------------------------------------------------
[Example]
S05_CORE_FLOW: agent_loop 主路径

[Description]
`agent_loop(messages, context)` 在 `agents/s20_comprehensive.py:1901-2004` 实现。

它先消费 cron 队列和后台通知，再根据 todo 计数注入提醒，随后执行上下文预算、更新 context、合并工具池、调用模型。

若模型没有 tool_use，触发 Stop hook 并返回；

若有工具，则逐个处理 compact、权限、后台执行和同步 handler。
[UML]
@startuml
start
:consume_cron_queue 并注入 Scheduled prompt;
:inject_background_notifications;
if (rounds_since_todo >= 3?) then (是)
  :插入 todo reminder;
endif
:prepare_context 原地压缩 messages;
:update_context 并 assemble_tool_pool;
:call_llm with_retry;
if (异常?) then (prompt too long 首次)
  :reactive_compact;
  :继续下一轮;
else (其他异常)
  :写入 assistant error;
  stop
endif
if (stop_reason == max_tokens?) then (是)
  if (首次超限?) then (是)
    :max_tokens 升级到 16000;
    :继续下一轮;
  else (否)
    :追加 CONTINUATION_PROMPT 或返回;
  endif
endif
:追加 assistant response;
if (没有 tool_use?) then (是)
  :trigger_hooks Stop;
  stop
endif
while (遍历 tool_use block)
  if (block.name == compact?) then (是)
    :compact_history 并跳过工具执行;
    :继续下一轮;
  elseif (PreToolUse 阻止?) then (是)
    :生成 denied tool_result;
  elseif (should_run_background?) then (是)
    :start_background_task;
    :返回占位 tool_result;
  else (同步工具)
    :call_tool_handler;
    :PostToolUse hook;
  endif
endwhile
:build_user_content(results);
:追加 user tool_result content;
repeat
@enduml

[Detail]
活动图回答“每轮 agent loop 如何前进，以及哪些条件会改变路径”。

重要分支包括 prompt too long 的 reactive compact、max_tokens 的一次升级、compact 工具的主动压缩、权限 hook 拦截、后台任务占位返回。

箭头和循环表示控制流，不表示线程边界。

维护时要注意 `messages` 被多处原地修改，任何新增分支都应保证最终要么 append tool_result，要么明确 continue/return。

------------------------------------------------------------
[Example]
错误恢复和降级路径

[Description]
`with_retry` 对 429 做指数退避，对 529/overloaded 累积计数并在配置 `FALLBACK_MODEL_ID` 时切换模型。

`is_prompt_too_long_error` 识别 prompt/context 过长，`agent_loop` 只允许一次 reactive compact。

`response.stop_reason == "max_tokens"` 会先提升输出 token，再最多追加两次 continuation prompt。
[UML]
None

[Detail]
这些恢复策略覆盖 API 速率、服务过载、上下文过长和输出截断四类问题。

它们没有持久化恢复状态，因此每次进入 `agent_loop` 都从新的 `RecoveryState` 开始；

这符合教学示例，但生产系统通常需要更细粒度的错误分类、观测指标和可配置退避策略。

------------------------------------------------------------
