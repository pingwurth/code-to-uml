Title: 顶层结构
Describe: 该文件按教学主题顺序组织，从任务系统和 worktree 起步，最后汇入主 Agent loop。
------------------------------------------------------------
[Example]
S02_TOP_LEVEL_STRUCTURE: 文件分区

[Description]
顶层结构可以按源码注释和函数位置分成 12 个区域：任务系统 `Task/create_task/claim_task/complete_task`，worktree 系统，技能加载，prompt 组装，基础工具，消息总线和协议状态，自主队友线程，hook 权限管道，子代理工具，上下文压缩和错误恢复，后台任务和 cron，MCP 和工具池，最后是 `agent_loop` 与 CLI 入口。
[UML]
@startuml
left to right direction
package "agents/s20_comprehensive.py" as File {
  component "任务系统\nTask + .tasks" as Tasks
  component "worktree 系统\n.worktrees + git" as WT
  component "技能和 prompt\nSKILL_REGISTRY + PROMPT_SECTIONS" as Prompt
  component "工具层\nrun_* + BUILTIN_TOOLS" as Tools
  component "协作协议\nMessageBus + ProtocolState" as Protocol
  component "上下文和恢复\ncompact + RecoveryState" as Recovery
  component "调度层\nbackground + cron" as Scheduling
  component "MCP 扩展\nMCPClient + assemble_tool_pool" as MCP
  component "主循环\nagent_loop + CLI" as Loop
}
Loop --> Prompt : 每轮重建 system prompt
Loop --> Tools : 执行 tool_use
Loop --> Recovery : 重试和压缩
Loop --> Scheduling : 注入后台/cron 通知
Tools --> Tasks : 任务读写
Tools --> WT : worktree 操作
Tools --> Protocol : 队友和 inbox
MCP --> Tools : 动态扩展工具池
Protocol --> Loop : 消息进入 history
@enduml

[Detail]
这张组件图回答“单文件里有哪些子系统，以及依赖方向如何流向主循环”。

箭头表示调用或运行时依赖方向：`agent_loop` 是汇聚点，工具层再分发到任务、worktree、消息协议和 MCP。

`MCPClient` 不直接参与模型调用，而是通过 `assemble_tool_pool` 把 prefixed tool 合并进工具定义和 handler 表。

导入期副作用包括读取 `.env`、创建 Anthropic client、创建 `.tasks`、`.worktrees`、`.mailboxes`，扫描 skills，加载 durable cron 并启动 cron scheduler 线程。

这些副作用让文件适合教学运行，但也意味着导入模块本身就会改变工作目录状态。

------------------------------------------------------------
[Example]
常量和全局状态速览

[Description]
关键配置常量包括 `DEFAULT_MAX_TOKENS=8000`、`ESCALATED_MAX_TOKENS=16000`、`MAX_RETRIES=3`、`CONTEXT_LIMIT=50000`、`PERSIST_THRESHOLD=30000`。

关键全局状态包括 `SKILL_REGISTRY`、`BUS`、`active_teammates`、`pending_requests`、`HOOKS`、`background_tasks/background_results`、`scheduled_jobs/cron_queue`、`mcp_clients`、`rounds_since_todo` 和 `agent_lock`。
[UML]
None

[Detail]
这些状态分散在多个区域，但都被主循环间接读取。

维护时最容易遗漏的是：`assemble_system_prompt` 会读取 skills、MCP 和 active teammates；

`prepare_context` 会原地修改 `messages`；

`assemble_tool_pool` 每轮重新合并 MCP 工具；

`cron_autorun_loop` 和 CLI 主循环都可能进入 `agent_loop`，因此主线程路径用 `agent_lock` 串行化。

------------------------------------------------------------
