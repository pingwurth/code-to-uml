Title: 整体架构图
Describe: 单文件内的运行时边界包括 CLI 主线程、cron 线程、队友线程、后台工具线程、Anthropic API 和文件系统状态。
------------------------------------------------------------
[Example]
S04_ARCHITECTURE: 运行时边界

[Description]
架构不是传统多模块分层，而是一个教学 harness 的运行时组合：主 CLI 线程把用户输入放入 `history`，`agent_loop` 调用 Anthropic API，模型返回 tool_use 后由本地 handler 执行。

cron scheduler 和 cron autorun 分别负责发现定时任务和自动驱动 loop。

队友线程和后台工具线程通过共享文件/字典把结果送回主循环。
[UML]
@startuml
left to right direction
actor "用户 CLI" as User
node "主进程" as Proc {
  component "__main__ 输入循环" as CLI
  component "agent_loop" as Loop
  component "cron_autorun_loop" as AutoCron
  component "cron_scheduler_loop" as Cron
  component "spawn_teammate_thread.run" as Mate
  component "background worker" as BG
  component "HOOKS 权限/日志" as Hooks
}
cloud "Anthropic Messages API" as API
database ".tasks\n.worktrees\n.mailboxes\n.transcripts\n.memory\n.scheduled_tasks.json" as FS
component "mock MCP servers\ndocs/deploy" as MCP
User --> CLI
CLI --> Loop
AutoCron --> Loop
Loop --> API
Loop --> Hooks
Loop --> FS
Loop --> MCP
Cron --> FS
Mate --> API
Mate --> FS
BG --> FS
MCP --> Loop
@enduml

[Detail]
阅读入口从左侧 CLI 开始。

箭头表示调用、读写或注入方向：主循环是唯一执行模型工具的核心路径；

cron 自动运行路径也通过 `agent_lock` 进入同一个 `agent_loop`。

文件系统数据库节点代表教学版的持久状态集合。

风险点在于多个 daemon thread 共享全局字典和文件，只有 cron 和 background 局部使用锁，任务和 mailbox 文件没有完整事务。

------------------------------------------------------------
