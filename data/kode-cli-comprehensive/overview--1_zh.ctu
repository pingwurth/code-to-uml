Title: 概览
Describe: 项目定位、运行模式、源码边界和学习目标。该报告面向源码学习、日常开发、架构复盘和面试讲解四种场景。
------------------------------------------------------------
[Example]
文件/目标概览：Kode CLI 解决什么问题

[Description]
Kode CLI 是一个面向终端的 agent runtime。它把用户输入、项目上下文、系统提示词、模型调用、工具执行、权限控制、MCP/插件扩展、Ink TUI 和会话持久化组织成一条可递归的消息循环。

从 `package.json` 看，包名是 `@shareai-lab/kode`，bin 包括 `kode`、`kwa`、`kd` 和 `kode-acp`；开发时依赖 Bun，发布后的 npm runtime 通过 Node 执行 `dist/index.js`，CLI shim 会优先使用缓存的 standalone binary。源码入口集中在 `src/entrypoints/`，核心编排在 `src/app/query.ts`，工具实现集中在 `src/tools/`。

本手册的目标不是复述目录，而是建立一套可用于复盘的 mental model：**入口负责接入协议，query 循环负责对话推进，Tool 契约负责能力边界，Permission Engine 负责风险控制，服务层负责模型/MCP/插件/上下文适配，UI/stdio/ACP 只是不同表现层。**

[UML]
@startmindmap
* Kode CLI 源码学习目标
** 解决的问题
*** 终端中驱动 LLM agent
*** 安全执行文件、Shell、搜索、MCP 工具
*** 同时支持交互 TUI、print、stream-json、ACP、MCP server
** 边界
*** 不直接改变外部 CLI 协议
*** 源码分析保持只读
*** 报告聚焦 src、scripts、tests、docs 中的架构证据
** 关键入口
*** src/entrypoints/cli.tsx
*** src/entrypoints/cli/runCli.tsx
*** src/entrypoints/acp.ts
*** src/entrypoints/mcp.ts
** 核心循环
*** src/app/query.ts
*** src/services/ai/llm.ts
*** src/core/permissions/engine/index.ts
*** src/tools/index.ts
** 学习产出
*** 开发上手
*** 架构复盘
*** 面试问答
*** 维护速查
@endmindmap

[Detail]
这张 mindmap 把项目拆成四个观察角度：

- **问题域**：Kode CLI 不是单纯的 CLI 参数解析器，而是一个多协议 agent runtime。
- **源码边界**：当前报告只分析源码和工程结构，不改动业务代码，也不声明 CLI 行为变化。
- **核心路径**：入口、query 循环、AI 服务、权限引擎、工具注册是理解项目的主干。
- **可复盘产出**：后续章节会把这些主干映射到类图、活动图、时序图和维护清单。

面试讲解时可以用一句话概括：**这个项目的难点在于把不稳定的模型输出、异步工具执行和高风险本地权限收敛进一个可流式渲染、可持久化、可扩展的对话循环。**

------------------------------------------------------------
[Example]
运行模式地图：一个 runtime，多种外壳

[Description]
Kode CLI 的运行面不是单一的 REPL。`runCli` 解析命令后，会根据参数和入口进入不同外壳：

- 默认交互模式：渲染 `src/ui/screens/REPL.tsx`，使用 Ink 展示消息、工具进度、权限确认和输入框。
- `--print` 文本/JSON/stream-json 模式：由 `src/entrypoints/cli/printMode.ts` 承接，适合脚本和 SDK 消费。
- ACP 模式：`src/entrypoints/acp.ts` 启动 JSON-RPC peer，`KodeAcpAgent` 把外部会话映射到 Kode 的消息循环。
- MCP server 模式：`src/entrypoints/mcp.ts` 把内置工具暴露成 MCP tool。
- 插件/MCP client 模式：CLI 运行时也会连接项目、用户和插件声明的 MCP server，并把外部工具纳入 `getTools()`。

[UML]
@startuml
left to right direction
skinparam componentStyle rectangle

actor "终端用户" as User
actor "脚本或 SDK" as Script
actor "ACP 客户端" as ACPClient
actor "MCP 客户端" as MCPConsumer

package "入口层 src/entrypoints" {
  component "cli.tsx" as CLI
  component "runCli.tsx" as RunCli
  component "printMode.ts" as Print
  component "acp.ts" as ACP
  component "mcp.ts" as MCP
}

package "共享 runtime" {
  component "query.ts\n消息循环" as Query
  component "llm.ts\n模型适配" as LLM
  component "tools/index.ts\n工具注册" as Tools
  component "permissions engine\n权限判定" as Perm
}

package "表现层" {
  component "REPL.tsx\nInk TUI" as REPL
  component "stdio protocol\nJSON/stream-json" as Stdio
  component "ACP JSON-RPC" as ACPProto
  component "MCP tool server" as MCPProto
}

User --> CLI
Script --> CLI
ACPClient --> ACP
MCPConsumer --> MCP

CLI --> RunCli
RunCli --> REPL : 默认交互
RunCli --> Print : --print
ACP --> ACPProto
MCP --> MCPProto

REPL --> Query
Print --> Query
ACPProto --> Query
MCPProto --> Tools

Query --> LLM
Query --> Tools
Query --> Perm
Tools --> Perm
@enduml

[Detail]
这个图强调一个核心结论：**REPL、print、ACP、MCP 是外壳，`query` 和 Tool/Permission/LLM 才是 runtime 内核。**

开发时要先判断改动位于哪一层：

- 改 CLI flag 或输出协议，风险是破坏外部兼容性，项目 AGENTS 明确要求不能随意改变。
- 改 `query.ts`，风险是影响所有外壳，因为交互、print、ACP 都复用它。
- 改 Tool 契约或权限引擎，风险是安全边界和工具执行行为。
- 改 UI 渲染，多数情况下只影响 Ink 交互，但仍可能通过 `useCanUseTool` 影响权限体验。

------------------------------------------------------------
[Example]
源码规模与学习路径定位

[Description]
CodeGraph 当前索引显示：项目包含 588 个索引文件、6989 个节点、16001 条边，主要语言为 TypeScript/TSX。这个规模已经不适合从文件树逐个读起，应该从“入口 -> 核心循环 -> 工具/权限 -> 服务适配 -> UI/协议 -> 测试”的路径进入。

学习时推荐先读 `src/entrypoints/cli.tsx` 和 `src/entrypoints/cli/runCli.tsx` 建立启动链，再读 `src/app/query.ts` 理解 agent 递归循环，然后读 `src/core/tools/tool.ts`、`src/tools/index.ts`、`src/core/permissions/engine/index.ts` 理解工具和安全边界。最后按工作需要选择 AI、MCP、插件、UI 或 ACP 分支。

[UML]
@startwbs
* Kode CLI 阅读路线
** 第 1 层：入口
*** cli.tsx
*** runCli.tsx
*** printMode.ts
*** acp.ts
*** mcp.ts
** 第 2 层：运行内核
*** app/query.ts
*** app/ask.ts
*** utils/messages/core.ts
*** utils/session/autoCompactCore.ts
** 第 3 层：工具与权限
*** core/tools/tool.ts
*** tools/index.ts
*** core/permissions/engine/index.ts
*** tools/system/BashTool
*** tools/filesystem/FileReadTool
*** tools/filesystem/FileEditTool
** 第 4 层：服务适配
*** services/ai/llm.ts
*** services/mcp/client.ts
*** services/plugins/pluginRuntime.ts
*** services/context/kodeContext.ts
** 第 5 层：外壳与验证
*** ui/screens/REPL.tsx
*** acp/kodeAcpAgent.ts
*** scripts/build.mjs
*** tests/unit、integration、e2e
@endwbs

[Detail]
这个 WBS 是开发和面试的双用途路线：

- **开发上手**：从入口读到内核，再定位具体子系统，避免直接在 588 个文件中横跳。
- **面试复盘**：按层解释“为什么这样拆”，而不是只背函数名。
- **维护实践**：遇到 bug 时先判断它属于输入外壳、消息循环、工具执行、权限、模型适配、持久化或 UI 渲染中的哪一段。

后续章节的所有图和清单都沿用这条阅读顺序。

------------------------------------------------------------
