Title: 数据流
Describe: 消息、权限、文件读写、自动压缩和会话持久化的数据流/状态流图。重点回答“状态在哪里变，怎样被持久化和恢复”。
------------------------------------------------------------
[Example]
数据流/状态流图：消息生命周期

[Description]
消息是 Kode runtime 的核心数据载体。输入可能来自 REPL、print stdin、stream-json、ACP blocks 或 TaskTool subagent。消息经过 `processUserInput`、`createUserMessage`、`normalizeMessagesForAPI`、`queryLLM`、工具结果组装、session JSONL 持久化等阶段。

关键约束：

- progress 和 synthetic error 可能不会直接进入模型 API。
- tool_result 必须和 tool_use id 对齐。
- session continuation 依赖历史消息和 response state。
- auto-compact 会改变历史上下文，但要保留任务关键事实。

[UML]
@startuml
start
:外部输入\nREPL / stdin / stream-json / ACP / Task;
:processUserInput\nslash、bash、mentions、images;
:createUserMessage\n生成 Kode user message;
:持久化到 session JSONL;
:queryCore 准备 prompt;
:normalizeMessagesForAPI\n过滤 progress、合并 tool_result;
:queryLLM 调用 provider;
:assistant message\ntext、thinking、tool_use;
if (存在 tool_use?) then (是)
  :ToolUseQueue 执行工具;
  :progress message 流式输出;
  :tool_result 组装为 user message;
  :持久化 tool_result;
  :回到 queryCore 下一轮;
else (否)
  :assistant final;
  :记录 cost、usage、response id;
  stop
endif
@enduml

[Detail]
这张图说明消息并不是“一条 prompt 一条 response”的简单结构，而是一个可递归、可流式、可持久化的数据流。

开发时常见 bug：

- progress message 被错误送入 provider，污染上下文。
- tool_result 缺少对应 tool_use id，provider 拒绝请求或模型无法理解。
- stream-json 与 REPL 使用同一 Kode message，但输出映射不同，容易漏字段。
- auto-compact 后丢失刚读取的文件、当前任务或权限状态。

------------------------------------------------------------
[Example]
数据流/状态流图：权限状态从规则到持久化

[Description]
权限不是单次布尔值，而是一组来源不同、会随会话变化的状态：

- 初始来源：用户 settings、项目 settings、local settings、当前 project config。
- CLI 来源：`--allowedTools`、`--disallowedTools`、`--permission-mode`、`--add-dir`。
- 运行时来源：用户在 REPL/ACP 中选择 allow always、deny、accept edits。
- 持久化目标：`toolPermissionSettings` 会把 allow/deny/ask/directories 更新写回对应 scope。

[UML]
@startuml
skinparam componentStyle rectangle

database "user settings" as UserSettings
database "project settings" as ProjectSettings
database "local settings" as LocalSettings
database "current project config" as ProjectConfig
component "toolPermissionSettings.ts\nload and persist" as Settings
component "CLI permission flags" as Flags
component "PermissionContext\nsession state" as Context
component "Permission Engine\nhasPermissionsToUseTool" as Engine
component "UI PermissionRequest" as UI
component "ACP request_permission" as ACP
database "updated settings" as Persisted

UserSettings --> Settings
ProjectSettings --> Settings
LocalSettings --> Settings
ProjectConfig --> Settings
Settings --> Context
Flags --> Context
Context --> Engine
Engine --> UI : 交互模式需要确认
Engine --> ACP : ACP 模式需要确认
UI --> Context : allow once 或 persist
ACP --> Context : allow/deny
Context --> Settings : apply updates
Settings --> Persisted
@enduml

[Detail]
这个图解释了为什么权限问题不能只看当前 UI 选择。一次工具是否允许，可能同时受 settings、CLI flags、project config、permission mode、safe mode 和运行时选择影响。

排查建议：

- 先打印或观察当前 `toolPermissionContext`。
- 再确认 permission mode 是否覆盖了默认规则。
- 对 Bash/File/Web/MCP 分支分别检查匹配规则。
- 如果 print 模式失败，检查是否因为 `shouldAvoidPermissionPrompts` 导致不能询问。

------------------------------------------------------------
[Example]
数据流/状态流图：文件读取、编辑和 mtime freshness

[Description]
文件工具的安全设计不是单纯“给 Edit 权限”。`FileReadTool` 会记录读取文件的时间戳；`FileEditTool` 在编辑前检查文件是否已经读取、是否仍是同一个 mtime、old_string 是否唯一、是否 notebook、是否存在 symlink 和路径权限风险。

这个设计防止模型在没有看过最新文件内容的情况下盲改，也降低用户或其他进程同时修改文件时覆盖变更的风险。

[UML]
@startuml
start
:FileReadTool 读取文件;
:校验存在、非目录、非二进制或可处理格式、大小限制;
:记录 readFileTimestamps[path] = mtime;
:返回带行号文本或图片/PDF/notebook 内容;
if (模型请求 FileEditTool?) then (是)
  :校验 old_string 与 new_string;
  :校验文件存在且不是 notebook;
  if (文件未读?) then (是)
    :拒绝编辑，要求先 Read;
    stop
  else (否)
    :比较当前 mtime 和读取时 mtime;
  endif
  if (mtime 已变化?) then (是)
    :拒绝编辑，要求重新 Read;
    stop
  else (否)
    :检查 old_string 唯一出现;
    :检查路径权限和 symlink 风险;
    :应用编辑并保留编码和换行;
    :记录 edit reminder 和 patch snippet;
    stop
  endif
else (否)
  stop
endif
@enduml

[Detail]
这条状态流是文件编辑工具的核心安全机制。它把“模型读过什么”和“文件现在是否还是那个版本”绑定起来。

维护者应避免绕过这套机制：

- 不要让写类工具在没有读取记录的情况下直接改文件。
- 不要忽略 mtime freshness，否则用户未保存或外部进程修改可能被覆盖。
- 大文件、二进制、notebook、图片/PDF 等格式要保持专门处理路径。

------------------------------------------------------------
[Example]
数据流/状态流图：auto-compact 与会话恢复

[Description]
长对话会触发 `src/utils/session/autoCompactCore.ts`。它会判断 token 压力，使用 compact/main model 生成摘要，恢复 selected files 等必要上下文，然后清理部分缓存和文件 freshness。

会话恢复则依赖 session JSONL、response state、conversation id 和历史消息。`runCli` 中的 resume/continue/fork/session-id 逻辑决定是继续同一会话、复制分叉，还是加载指定会话。

[UML]
@startuml
state "正常对话" as Normal
state "Token 接近阈值" as Threshold
state "生成压缩摘要" as Summarize
state "恢复关键上下文" as Restore
state "继续 query 循环" as Continue
state "会话写入 JSONL" as Persist
state "resume / continue / fork" as Resume

[*] --> Normal
Normal --> Persist : 每轮消息追加
Normal --> Threshold : token pressure
Threshold --> Summarize : compact model 或 main model
Summarize --> Restore : selected files、任务事实、上下文
Restore --> Continue : 替换或收缩历史消息
Continue --> Normal
Persist --> Resume : 用户下次恢复
Resume --> Normal : 加载 messages 和 response state
@enduml

[Detail]
auto-compact 的设计取舍是：牺牲完整历史，保留继续任务所需事实。它不是纯 UI 行为，影响后续 provider 请求和工具上下文。

开发时需要关注：

- compact 后是否丢失刚生成的计划、文件路径、未完成工具结果。
- selected files 是否正确恢复。
- response state 和 provider continuation 是否与压缩后的消息兼容。
- fork/resume/continue 是否正确区分会话身份和历史复制。

------------------------------------------------------------
