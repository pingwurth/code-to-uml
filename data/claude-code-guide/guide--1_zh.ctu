Title: 开发上手指南
Describe: 推荐阅读顺序、开发环境搭建、调试技巧、扩展入口、常见问题 Q&A、维护者速查表。
------------------------------------------------------------
[Example]
推荐阅读顺序

[Description]
建议按以下顺序阅读源码，从入口到底层，从核心到扩展：

**第一阶段：理解入口和主循环**（1-2 天）
1. `src/entrypoints/cli.tsx` — 启动入口，理解快速路径设计
2. `src/main.tsx` — Commander.js 命令定义（先跳过子命令，聚焦 .action() 主处理器）
3. `src/entrypoints/init.ts` — 初始化流程
4. `src/screens/REPL.tsx` — 交互式 UI（先看顶层结构，不深入组件细节）

**第二阶段：核心引擎**（2-3 天）
5. `src/QueryEngine.ts` — 会话生命周期管理
6. `src/query.ts` — 单次 turn 的执行逻辑（核心！）
7. `src/Tool.ts` — 工具接口定义
8. `src/tools.ts` — 工具注册与组装

**第三阶段：API 与工具**（2-3 天）
9. `src/services/api/claude.ts` — API 客户端（3558 行，重点看 queryModelStream）
10. `src/services/tools/StreamingToolExecutor.ts` — 流式工具执行
11. `packages/builtin-tools/src/tools/` — 选几个工具看实现（BashTool、FileEditTool、AgentTool）

**第四阶段：状态与上下文**（1-2 天）
12. `src/state/store.ts` — 状态管理
13. `src/bootstrap/state.ts` — 会话全局状态
14. `src/context.ts` — Context 组装

[UML]
@startmindmap
* 阅读路线图
** 第一阶段: 入口 (1-2天)
*** cli.tsx → main.tsx → init.ts → REPL.tsx
*** 关注: 启动流程、命令分发
** 第二阶段: 引擎 (2-3天)
*** QueryEngine → query.ts → Tool.ts → tools.ts
*** 关注: 对话循环、工具接口
** 第三阶段: API & 工具 (2-3天)
*** claude.ts → StreamingToolExecutor → 具体工具
*** 关注: 流式处理、并发执行
** 第四阶段: 状态 (1-2天)
*** store.ts → bootstrap/state.ts → context.ts
*** 关注: 状态管理、上下文组装
** 第五阶段: 扩展 (按需)
*** MCP、Bridge、Daemon、ACP
*** Feature Flag 系统
@endmindmap

[Detail]
**阅读技巧**：

1. **先看类型，后看实现**: TypeScript 项目的类型定义是最好的文档。`Tool`、`Message`、`AppState` 等类型比实现代码更能揭示设计意图
2. **追踪数据流**: 从用户输入开始，追踪 Message 如何流经系统
3. **关注边界**: 模块之间的接口（函数签名、类型定义）比内部实现更重要
4. **善用 CodeGraph**: `codegraph_context` 和 `codegraph_explore` 可以快速找到符号的定义、调用者、被调用者

------------------------------------------------------------
[Example]
开发环境搭建

[Description]
```bash
# 1. 克隆仓库
git clone <repo-url>
cd claude-code

# 2. 安装依赖
bun install

# 3. 开发模式运行
bun run dev

# 4. 运行测试
bun test

# 5. 完整检查 (typecheck + lint + test)
bun run precheck

# 6. 构建
bun run build

# 7. 代码检查
bun run check:fix    # lint + format auto-fix
bun run lint         # lint only
bun run format       # format only
```

**关键环境变量**:
- `ANTHROPIC_API_KEY` — API 密钥（必需）
- `FEATURE_<NAME>=1` — 启用 feature flag
- `CLAUDE_CODE_USE_OPENAI=1` — 使用 OpenAI 兼容层
- `CLAUDE_CODE_USE_GEMINI=1` — 使用 Gemini 兼容层

[UML]
@startmindmap
* 开发工作流
** 日常开发
*** bun run dev (热重载)
*** bun test --watch (测试监听)
** 提交前
*** bun run precheck (完整检查)
*** pre-commit hook 自动执行
** 构建发布
*** bun run build
*** bun run build:vite (备选)
*** node dist/cli.js (验证产物)
@endmindmap

[Detail]
**常见开发场景**：

**添加新工具**:
1. 在 `packages/builtin-tools/src/tools/` 创建新目录
2. 实现 `Tool` 接口（name、inputSchema、call、description 等）
3. 在 `src/tools.ts` 导入并注册
4. 如果是延迟工具，添加 `searchHint` 和到 `CORE_TOOLS` 白名单外
5. 编写测试 `__tests__/` 目录
6. 运行 `bun run precheck` 确认通过

**添加新 Feature Flag**:
1. 在 `scripts/defines.ts` 的 `DEFAULT_BUILD_FEATURES` 添加 flag 名
2. 在代码中使用 `import { feature } from 'bun:bundle'` + `if (feature('FLAG'))`
3. 使用 `require()` 条件导入相关模块（不要用静态 `import`）
4. 运行 `FEATURE_FLAG=1 bun run dev` 验证

**调试技巧**:
- `bun run dev:inspect` — 启用 Node.js inspector（设置 `BUN_INSPECT=9229` 选端口）
- `--verbose` — 详细日志输出
- `profileCheckpoint()` — 启动性能分析
- `logForDebugging()` — 调试日志（不发送到分析系统）
- `logError()` — 错误日志（发送到分析系统）

------------------------------------------------------------
[Example]
常见问题 Q&A

[Description]
**Q1: 为什么使用 Bun 而不是 Node.js？**
A: Bun 提供更快的启动速度（JIT vs V8）、内置 bundler（无需 webpack）、内置测试框架（无需 jest）。对于 CLI 工具，启动速度是核心体验指标。

**Q2: 为什么要 fork Ink 而不是用原版？**
A: 原版 Ink 缺少 keybindings、theme 系统、selection 等终端特性。Fork 版本在 `packages/@ant/ink/` 维护。

**Q3: 为什么 `feature()` 不能赋值给变量？**
A: Bun 编译器在构建时将 `feature()` 替换为布尔字面量。只有直接用在 `if` 条件中才能实现死代码消除。赋值给变量后替换为 `const x = true`，失去了 tree-shaking 能力。

**Q4: 如何理解 `mock.module` 的全局污染问题？**
A: Bun 的 `mock.module` 是进程全局的（last-write-wins），不是 per-file 隔离的。一个测试文件的 mock 会污染同一进程中所有其他测试文件。解决方案：mock 底层依赖（如 axios）而非上层业务模块。

**Q5: 为什么构建产物必须代码分割？**
A: Bun/JSC 全量解析单个大 JS 文件的 bytecode，17MB 单文件导致 RSS ~1GB。分割为 600+ 小 chunk 后按需加载，`--version` RSS 从 966MB 降至 35MB。

**Q6: QueryEngine 为什么用 AsyncGenerator 而不是 Promise？**
A: 因为需要在 turn 执行过程中流式地 yield 中间结果（进度、工具结果、部分响应）给调用方。Promise 只能 resolve 一次，AsyncGenerator 可以 yield 多次。

[UML]
@startmindmap
* 常见问题
** 性能
*** 为什么用 Bun? (启动速度)
*** 为什么代码分割? (RSS 内存)
*** 为什么延迟工具加载? (token 成本)
** 架构
*** 为什么 fork Ink? (终端特性)
*** 为什么自实现 Store? (34行够用)
*** 为什么 AsyncGenerator? (流式 yield)
** 开发
*** feature() 为什么不能赋值? (tree-shaking)
*** mock.module 全局污染? (进程级)
*** 如何添加新工具? (Tool 接口)
@endmindmap

[Detail]
**Q7: 如何添加新的 API Provider？**
A: 在 `src/services/api/` 创建新目录，实现流适配器（请求转换 + 流适配 + 响应标准化）。在 `src/utils/model/providers.ts` 注册 provider。通过环境变量控制启用。

**Q8: MCP 工具和内置工具有什么区别？**
A: 内置工具编译在 bundle 中，MCP 工具通过外部 Server 动态发现。MCP 工具使用 JSON Schema（非 Zod），权限继承自 Server 配置，支持运行时动态添加/移除。

**Q9: 为什么 `bootstrap/state.ts` 使用模块级单例？**
A: 会话全局状态（session ID、CWD、cost tracking）需要跨模块共享且生命周期与进程一致。使用模块单例避免了 prop drilling 和 context 嵌套。注释明确警告 "DO NOT ADD MORE STATE HERE"。

------------------------------------------------------------
[Example]
维护者速查表

[Description]
**核心文件索引**:

| 文件 | 行数 | 职责 |
|------|------|------|
| `src/entrypoints/cli.tsx` | ~200 | 启动入口，快速路径分发 |
| `src/main.tsx` | 5674 | Commander.js CLI 定义，子命令注册 |
| `src/entrypoints/init.ts` | ~200 | 初始化（配置、遥测、Trust 对话框） |
| `src/screens/REPL.tsx` | 6541 | 交互式终端 UI |
| `src/QueryEngine.ts` | 1365 | 会话生命周期管理 |
| `src/query.ts` | 2042 | 单次 turn 执行逻辑 |
| `src/Tool.ts` | 813 | 工具接口定义 |
| `src/tools.ts` | 419 | 工具注册与组装 |
| `src/context.ts` | 189 | Context 组装 |
| `src/services/api/claude.ts` | 3558 | API 客户端 |
| `src/services/tools/StreamingToolExecutor.ts` | ~200 | 流式工具执行器 |
| `src/state/store.ts` | 34 | 状态管理 |
| `src/bootstrap/state.ts` | ~200 | 会话全局状态 |

**关键类型文件**:

| 文件 | 内容 |
|------|------|
| `src/types/message.ts` | Message 类型层次 |
| `src/types/permissions.ts` | 权限模式和结果类型 |
| `src/types/tools.ts` | 工具进度数据类型 |
| `src/types/global.d.ts` | MACRO、BUILD_TARGET 声明 |
| `src/types/internal-modules.d.ts` | bun:bundle 类型声明 |

**关键常量文件**:

| 文件 | 内容 |
|------|------|
| `src/constants/tools.ts` | CORE_TOOLS 白名单 (38 个) |
| `src/constants/prompts.ts` | System prompt 模板 |
| `src/constants/system.ts` | 系统常量（归属头、CLI 前缀） |
| `scripts/defines.ts` | 构建常量（版本号、feature flags） |

[UML]
@startmindmap
* 速查索引
** 入口 & 启动
*** cli.tsx (快速路径)
*** main.tsx (命令定义)
*** init.ts (初始化)
** 核心引擎
*** QueryEngine.ts (会话管理)
*** query.ts (turn 执行)
*** Tool.ts (工具接口)
** API 层
*** claude.ts (主客户端)
*** openai/ gemini/ grok/ (兼容层)
** 工具系统
*** tools.ts (注册)
*** StreamingToolExecutor.ts (执行)
*** builtin-tools/ (60+ 实现)
** 状态 & UI
*** store.ts (状态管理)
*** REPL.tsx (主界面)
*** components/ (149 组件)
** 配置 & 构建
*** defines.ts (构建常量)
*** build.ts (构建脚本)
*** biome.json (lint 配置)
@endmindmap

[Detail]
**快速定位代码的技巧**：

1. **找定义**: `codegraph_search` + `codegraph_node`
2. **找调用者**: `codegraph_callers`
3. **找被调用者**: `codegraph_callees`
4. **影响分析**: `codegraph_impact`
5. **文件结构**: `codegraph_files`
6. **文本搜索**: `grep` (用于搜索字符串、注释、日志消息)

**常用命令**:
```bash
bun run precheck          # 完整检查 (typecheck + lint + test)
bun test path/to/file     # 运行单个测试文件
bun run dev               # 开发模式
bun run build             # 构建
bun run check:fix         # 自动修复 lint/format
```
