Title: 核心流程图
Describe: 主执行路径、重要分支、失败/错误路径、重试和回退行为
------------------------------------------------------------
[Example]
CLI 启动流程

[Description]
<!-- Section-ID: S05_CORE_FLOW -->
Claude Code Best 的启动流程：

**快速路径**：
1. 检查 --version/-v 参数
2. 检查 --dump-system-prompt 参数
3. 检查 --claude-in-chrome-mcp 参数
4. 检查 --computer-use-mcp 参数
5. 检查 --daemon-worker 参数

**完整启动**：
1. 加载 main.tsx
2. Commander.js CLI 定义
3. 注册子命令
4. 处理权限、MCP、会话恢复
5. 分发 REPL/Headless 模式

[UML]
@startuml
start
:检查命令行参数;
if (快速路径?) then (是)
  :处理快速路径;
  stop
else (否)
  :加载 main.tsx;
  :Commander.js CLI 定义;
  :注册子命令;
  :处理权限、MCP、会话恢复;
  :分发 REPL/Headless 模式;
  stop
endif
@enduml

[Detail]
**启动流程详解**：

1. **快速路径**：
   - --version/-v：直接输出版本号，零模块加载
   - --dump-system-prompt：输出系统 prompt，feature-gated
   - --claude-in-chrome-mcp：Chrome MCP 模式
   - --computer-use-mcp：Computer Use MCP 模式
   - --daemon-worker：Daemon 模式

2. **完整启动**：
   - 加载 main.tsx（~5674 行）
   - Commander.js CLI 定义
   - 注册子命令（mcp、server、ssh、open、auth 等）
   - 处理权限、MCP、会话恢复
   - 分发 REPL/Headless 模式

------------------------------------------------------------
[Example]
API 查询流程

[Description]
<!-- Section-ID: S05_CORE_FLOW -->
核心 API 查询流程：

**查询准备**：
1. 构建系统 prompt
2. 准备消息历史
3. 准备工具列表
4. 设置 API 参数

**流式响应**：
1. 发送 API 请求
2. 接收流式响应
3. 处理文本增量
4. 处理工具调用

**工具执行**：
1. 解析工具调用
2. 验证工具权限
3. 执行工具
4. 返回工具结果

[UML]
@startuml
start
:构建系统 prompt;
:准备消息历史;
:准备工具列表;
:设置 API 参数;
:发送 API 请求;
:接收流式响应;
while (有流式事件?) is (是)
  :处理文本增量;
  if (工具调用?) then (是)
    :解析工具调用;
    :验证工具权限;
    :执行工具;
    :返回工具结果;
  endif
endwhile (否)
:完成查询;
stop
@enduml

[Detail]
**查询流程详解**：

1. **查询准备**：
   - 构建系统 prompt（包含上下文、工具说明等）
   - 准备消息历史（压缩、归档）
   - 准备工具列表（条件加载）
   - 设置 API 参数（模型、温度、max_tokens 等）

2. **流式响应**：
   - 发送 API 请求到 Anthropic SDK
   - 接收流式响应（BetaRawMessageStreamEvent）
   - 处理文本增量（实时显示）
   - 处理工具调用（解析、验证、执行）

3. **工具执行**：
   - 解析工具调用（JSON Schema 验证）
   - 验证工具权限（用户确认）
   - 执行工具（本地/远程）
   - 返回工具结果（文本/错误）

------------------------------------------------------------
[Example]
错误处理流程

[Description]
<!-- Section-ID: S05_CORE_FLOW -->
错误处理和重试机制：

**API 错误**：
1. 网络错误：重试 3 次
2. 速率限制：指数退避
3. 认证错误：提示重新登录
4. 模型错误：切换模型

**工具错误**：
1. 权限错误：提示用户授权
2. 执行错误：返回错误信息
3. 超时错误：取消执行
4. 资源错误：清理资源

**状态错误**：
1. 会话过期：重新认证
2. 状态损坏：重置状态
3. 内存不足：压缩历史
4. 并发冲突：序列化执行

[UML]
@startuml
start
:检测错误类型;
switch (错误类型)
case (API 错误)
  :重试/退避/切换模型;
case (工具错误)
  :权限/执行/超时/资源处理;
case (状态错误)
  :会话/状态/内存/并发处理;
endswitch
:记录错误日志;
:恢复执行;
stop
@enduml

[Detail]
**错误处理详解**：

1. **API 错误**：
   - 网络错误：重试 3 次，指数退避
   - 速率限制：等待后重试
   - 认证错误：提示重新登录
   - 模型错误：切换到备用模型

2. **工具错误**：
   - 权限错误：提示用户授权
   - 执行错误：返回错误信息给用户
   - 超时错误：取消执行，清理资源
   - 资源错误：释放占用的资源

3. **状态错误**：
   - 会话过期：重新认证
   - 状态损坏：重置状态
   - 内存不足：压缩历史消息
   - 并发冲突：序列化执行

------------------------------------------------------------
