Title: 数据流/状态流图
Describe: 输入、转换、持久化、输出、状态所有权和生命周期转换
------------------------------------------------------------
[Example]
消息数据流

[Description]
<!-- Section-ID: S07_DATA_OR_STATE_FLOW -->
Claude Code Best 的消息数据流：

**用户输入**：
1. 用户输入消息
2. 创建 UserMessage
3. 添加到消息历史
4. 发送到 API

**API 响应**：
1. 接收流式响应
2. 创建 AssistantMessage
3. 处理文本增量
4. 处理工具调用

**工具结果**：
1. 执行工具
2. 创建 ToolResultMessage
3. 添加到消息历史
4. 继续查询

[UML]
@startuml
start
:用户输入消息;
:创建 UserMessage;
:添加到消息历史;
:发送到 API;
:接收流式响应;
:创建 AssistantMessage;
:处理文本增量;
if (工具调用?) then (是)
  :执行工具;
  :创建 ToolResultMessage;
  :添加到消息历史;
  :继续查询;
else (否)
  :完成查询;
endif
stop
@enduml

[Detail]
**消息数据流详解**：

1. **用户输入**：
   - 用户在终端输入消息
   - 创建 UserMessage 对象
   - 添加到消息历史数组
   - 发送到 API 进行处理

2. **API 响应**：
   - 接收流式响应（BetaRawMessageStreamEvent）
   - 创建 AssistantMessage 对象
   - 处理文本增量（实时显示）
   - 处理工具调用（解析、验证、执行）

3. **工具结果**：
   - 执行工具（本地/远程）
   - 创建 ToolResultMessage 对象
   - 添加到消息历史数组
   - 继续查询（直到没有工具调用）

------------------------------------------------------------
[Example]
状态生命周期

[Description]
<!-- Section-ID: S07_DATA_OR_STATE_FLOW -->
应用状态生命周期：

**会话状态**：
1. 创建会话（session ID）
2. 加载配置（globalConfig）
3. 初始化状态（AppState）
4. 运行查询（query）
5. 保存会话（sessionStorage）

**工具状态**：
1. 注册工具（tools.ts）
2. 加载工具（条件加载）
3. 验证工具（Tool.validate）
4. 执行工具（Tool.execute）
5. 返回结果（ToolResult）

**权限状态**：
1. 检查权限（permission check）
2. 请求授权（user confirmation）
3. 记录权限（denial tracking）
4. 应用权限（permission mode）

[UML]
@startuml
[*] --> 会话创建
会话创建 --> 配置加载
配置加载 --> 状态初始化
状态初始化 --> 查询运行
查询运行 --> 会话保存
会话保存 --> [*]

[*] --> 工具注册
工具注册 --> 工具加载
工具加载 --> 工具验证
工具验证 --> 工具执行
工具执行 --> 结果返回
结果返回 --> [*]

[*] --> 权限检查
权限检查 --> 用户授权
用户授权 --> 权限记录
权限记录 --> 权限应用
权限应用 --> [*]
@enduml

[Detail]
**状态生命周期详解**：

1. **会话状态**：
   - 创建会话：生成 session ID
   - 加载配置：读取 globalConfig
   - 初始化状态：创建 AppState
   - 运行查询：执行 API 查询
   - 保存会话：写入 sessionStorage

2. **工具状态**：
   - 注册工具：tools.ts 注册所有工具
   - 加载工具：根据 feature() 标志条件加载
   - 验证工具：Tool.validate() 验证输入
   - 执行工具：Tool.execute() 执行工具
   - 返回结果：返回 ToolResult

3. **权限状态**：
   - 检查权限：检查工具权限
   - 请求授权：提示用户确认
   - 记录权限：记录权限决策
   - 应用权限：应用权限模式

------------------------------------------------------------
