Title: 整体架构图
Describe: 6 层架构从存储层到交互层，4 种线程实现并发。
------------------------------------------------------------
[Example]
S04_ARCHITECTURE: 六层架构与依赖方向

[Description]
系统分为 6 层，依赖方向从上到下（上层调用下层）：

| 层 | 职责 | 关键组件 |
|----|------|----------|
| 交互层 | CLI 输入/输出、终端显示 | `__main__`、`terminal_print()`、`PROMPT` |
| 控制层 | 循环编排、错误恢复、上下文管理 | `agent_loop()`、`RecoveryState`、`prepare_context()` |
| 集成层 | 外部服务连接 | `MCPClient`、`assemble_tool_pool()`、`assemble_system_prompt()` |
| 执行层 | 工具调用、权限检查、后台任务 | `BUILTIN_HANDLERS`、`permission_hook()`、`start_background_task()` |
| 通信层 | 跨线程消息传递 | `MessageBus`、`ProtocolState`、`pending_requests` |
| 存储层 | 文件持久化 | `Task`、`CronJob`、worktree、skills、memory |
[UML]
@startuml
left to right direction

package "交互层" {
  [CLI 入口\n__main__] as CLI
  [terminal_print] as TP
}

package "控制层" {
  [agent_loop\n主循环] as LOOP
  [prepare_context\n上下文预算] as CTX
  [RecoveryState\n错误恢复] as ERR
  [Teammate Thread\n队友线程] as TEAM
}

package "集成层" {
  [assemble_tool_pool\n工具池合并] as POOL
  [assemble_system_prompt\n提示装配] as PROMPT
  [MCPClient\nMCP 客户端] as MCP
  [Skill Loading\n技能注册表] as SKILL
}

package "执行层" {
  [BUILTIN_HANDLERS\n工具处理器] as HAND
  [permission_hook\n权限管线] as PERM
  [start_background_task\n后台任务] as BG
  [spawn_subagent\n子 Agent] as SUB
}

package "通信层" {
  [MessageBus\nJSONL 邮箱] as BUS
  [ProtocolState\n协议匹配] as PROTO
  [active_teammates\n队友注册表] as REG2
}

package "存储层" {
  [Task System\n.tasks] as TASK
  [CronJob\n.scheduled_tasks.json] as CRON
  [Worktree\n.worktrees] as WT
  [Memory\n.memory] as MEM
  [Skills\nskills] as SK
  [Transcripts\n.transcripts] as TRANS
}

CLI --> LOOP
CLI --> TP
LOOP --> CTX
LOOP --> ERR
LOOP --> POOL
LOOP --> HAND
LOOP --> BG
LOOP --> BUS
LOOP --> CRON
TEAM --> LOOP
TEAM --> BUS
TEAM --> TASK
TEAM --> WT
POOL --> MCP
POOL --> SKILL
PROMPT --> SKILL
PROMPT --> MCP
PROMPT --> MEM
PROMPT --> BUS
HAND --> PERM
HAND --> TASK
HAND --> CRON
HAND --> WT
SUB --> HAND
SUB --> PERM
BG --> HAND
BUS --> REG
@enduml

[Detail]
**依赖方向约定**：箭头表示"调用/使用"方向。上层可以调用下层，下层通过回调（如 hook）或返回值向上层传递信息。

**层间通信方式**：
- 交互层 → 控制层：直接函数调用
- 控制层 → 执行层：通过 `handlers` 字典间接调用
- 执行层 → 通信层：通过全局 `BUS` 单例
- 通信层 → 存储层：通过文件 I/O

**架构特点**：
- 没有接口/抽象类，所有依赖通过模块级全局变量实现
- 这是教学简化：生产系统应使用依赖注入
- 存储层全部基于文件系统，没有数据库

------------------------------------------------------------
[Example]
S04_ARCHITECTURE: 四线程并发模型

[Description]
文件使用 4 种线程实现并发，通过 `threading.Lock` 保护共享状态：

| 线程 | 启动位置 | 职责 | 守护线程 |
|------|----------|------|----------|
| 主线程 | `__main__` | CLI 交互、agent_loop 执行 | 否 |
| cron_scheduler_loop | 第 1473 行 | 每秒检查 cron 作业 | 是 |
| cron_autorun_loop | 第 2040 行 | 消费 cron 队列，触发 agent_loop | 是 |
| teammate threads | 第 808 行 | 每个队友独立的消息循环 | 是 |

锁保护：
- `cron_lock`：保护 `scheduled_jobs`、`cron_queue`、`_last_fired`
- `background_lock`：保护 `background_tasks`、`background_results`
- `agent_lock`：保护 `agent_loop` 和 `cron_autorun_loop` 的互斥
[UML]
@startuml
start
fork
  :主线程
  CLI 交互循环
  agent_loop();
fork again
  :cron_scheduler_loop
  每秒检查 cron 表达式
  匹配则加入 cron_queue;
fork again
  :cron_autorun_loop
  消费 cron_queue
  获取 agent_lock
  执行 agent_loop();
fork again
  :teammate thread
  inbox 轮询
  模型调用
  工具执行;
end fork
stop
@enduml

[Detail]
**线程安全风险**：
- `active_teammates` 字典（第 502 行）没有锁保护，仅靠 GIL 保证基本原子性
- `pending_requests` 字典（第 517 行）同理
- `messages` 列表在 teammate 线程中被修改，但每个 teammate 有独立的 messages
- `history` 列表在主线程和 cron_autorun_loop 之间共享，通过 `agent_lock` 保护

**agent_lock 的作用**（第 1858 行）：确保同一时间只有一个线程在执行 `agent_loop()`。主线程在用户输入后获取锁，cron_autorun_loop 在注入定时任务时获取锁。
