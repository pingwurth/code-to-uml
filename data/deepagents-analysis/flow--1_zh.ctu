Title: 核心流程
Describe: Agent 创建、任务执行、CLI 部署的关键执行路径
------------------------------------------------------------
[Example]
Agent 创建流程

[Description]
`create_deep_agent()` 内部的组装步骤。
830行函数的核心逻辑分解为7个阶段。

[UML]
@startuml
start

:resolve_model(model);
note right: 字符串解析为 BaseChatModel\n或使用默认 claude-sonnet-4-6

:_harness_profile_for_model(model);
note right: 查找匹配的 HarnessProfile\n决定 system_prompt_suffix 等配置

:_validate_excluded_middleware_config();
note right: 检查 profile 中 excluded_middleware\n不包含必需中间件

:assemble system_prompt;
note right: USER + (BASE|CUSTOM) + SUFFIX\n三段式拼接

:build middleware stack;
note right: base stack → user middleware → tail stack\n然后 apply excluded_middleware 过滤

:process subagents;
note right: 解析 SubAgent/CompiledSubAgent/AsyncSubAgent\n自动添加 general-purpose 子智能体

:create_agent() + with_config();
note right: 调用 LangChain create_agent\n设置 recursion_limit=9999

stop
@enduml

[Detail]
**阶段详解**:

1. **模型解析** (`resolve_model`): 支持 `"provider:model"` 字符串格式，如 `"openai:gpt-5.5"`。
`None` 触发废弃警告并使用默认 Anthropic 模型。

2. **Profile 查找**: 根据模型类型匹配注册的 `HarnessProfile`，决定：
   - 是否替换 BASE prompt
   - 添加什么 system_prompt_suffix
   - 排除哪些中间件/工具
   - 注入哪些额外中间件

3. **Excluded 验证**: 确保不会排除 `FilesystemMiddleware` 和 `SubAgentMiddleware`（安全关键中间件）。

4. **Prompt 组装**: 严格顺序 USER → BASE/CUSTOM → SUFFIX。
SystemMessage 支持 cache_control 标记。

5. **中间件栈构建**: Base → User → Tail，然后过滤 excluded。
详细顺序见架构总览。

6. **子智能体处理**: 分离同步/异步子智能体，各自继承父配置，构建子级中间件栈。

7. **最终编译**: 调用 `create_agent()` 获取 `CompiledStateGraph`，附加元数据（版本号、agent 名称）。

**关键决策点**:
- model=None 时是否发出废弃警告
- 是否添加默认 general-purpose 子智能体
- SystemMessage vs str 类型的 system_prompt 走不同拼接路径

------------------------------------------------------------
[Example]
任务执行流程 (invoke)

[Description]
Agent 处理用户消息的完整循环。
包含工具调用的反射循环（可能多次 LLM 调用）。

[UML]
@startuml
start

:接收用户消息;

:中间件前处理\n(pre-processing);
note right: 各中间件按顺序处理输入\nSkills加载、Memory注入等

:调用 LLM;

if (LLM 返回工具调用?) then (yes)
  :解析 tool_calls;
  :中间件拦截工具调用\n(FilesystemMW, SubAgentMW等);
  :执行工具操作;
  :收集工具结果;
  :将结果追加到消息历史;
  :再次调用 LLM;
  note right: 工具循环\n直到 LLM 不再请求工具
  -> 循环;
else (no)
  :返回文本响应;
endif

:中间件后处理\n(post-processing);
note right: Summarization 检查是否需要压缩\nMemory 持久化更新

:DeltaChannel 保存增量;

stop
@enduml

[Detail]
**中间件栈执行顺序**:

前处理（由外到内）：
1. `TodoListMiddleware` - 注入 todo 工具
2. `SkillsMiddleware` - 加载 skill 文件到 prompt
3. `FilesystemMiddleware` - 注入文件操作工具
4. `SubAgentMiddleware` - 注入 task 工具
5. `SummarizationMiddleware` - 上下文长度检查
6. `PatchToolCallsMiddleware` - 修复格式异常的 tool call

**工具调用循环**:
- LLM 可在一次响应中返回多个 tool_calls
- 每个 tool call 由对应中间件拦截处理
- 工具结果作为 `ToolMessage` 追加到历史
- 循环直到 LLM 响应中无 tool_calls（纯文本）
- `recursion_limit=9999` 允许极深的工具循环

**边界情况**:
- 工具执行失败：错误信息作为 ToolMessage 返回给 LLM
- 上下文溢出：SummarizationMiddleware 自动压缩历史消息
- 中断请求：HumanInTheLoopMiddleware 可暂停等待用户确认

------------------------------------------------------------
[Example]
CLI 部署流程

[Description]
`deepagents init` → `dev` → `deploy` 的端到端工作流。
从项目脚手架到云端部署。

[UML]
@startuml
start

partition "init" {
  :deepagents init;
  :选择模板\n(coding-agent / content-writer / custom);
  :生成项目脚手架;
  note right: agent.json\nAGENTS.md\nskills/\n.env.example
}

partition "dev" {
  :deepagents dev;
  :加载 .env 配置;
  :启动 langgraph dev server;
  :本地测试和调试;
}

partition "deploy" {
  :deepagents deploy;
  :bundler 打包项目文件;
  :context_hub 收集上下文;
  :上传到 LangGraph Platform;
  :验证部署状态;
}

stop
@enduml

[Detail]
**各阶段详解**:

**init 阶段**:
- 入口：`deepagents_cli/deploy/commands.py`
- 生成标准项目结构：`agent.json` (配置)、`AGENTS.md` (记忆)、`skills/` (技能)
- 模板系统在 `deepagents_cli/deploy/templates.py`

**dev 阶段**:
- 加载 `.env` 文件（`deepagents_cli/config.py` → `_load_dotenv`）
- 内部调用 `langgraph dev` 启动本地开发服务器
- 支持热重载，修改代码立即生效

**deploy 阶段**:
- `bundler.py`: 打包项目文件为部署包
- `context_hub.py`: 收集 AGENTS.md 和 skills 文件
- 使用 LangGraph Platform API 上传和部署
- 支持 trusted publishing (OIDC) 认证

**错误处理**:
- 缺少 API key：提示用户配置 `.env`
- 网络超时：自动重试 + 友好错误信息
- 配置验证失败：列出具体的配置问题

------------------------------------------------------------
