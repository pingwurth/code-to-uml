Title: 顶层结构
Describe: 覆盖 S02_TOP_LEVEL_STRUCTURE。梳理 libs/、partners、standard-tests、model-profiles、CI 工作流和包级依赖方向。
------------------------------------------------------------
[Example]
S02_TOP_LEVEL_STRUCTURE - monorepo 包结构

[Description]
`libs/` 是主要工程边界：`core` 提供 `langchain-core` 抽象，`langchain_v1` 发布活跃的 `langchain` 包，`langchain` 发布 `langchain-classic` legacy 包，`partners` 发布第三方 integration 包，`standard-tests` 发布 `langchain-tests` 标准测试，`text-splitters` 独立提供文档分块工具，`model-profiles` 提供 `langchain-profiles` CLI。每个包有自己的 `pyproject.toml` 和 `uv.lock`，`libs/Makefile` 的 `lock`/`check-lock` 覆盖 core、text-splitters、langchain、langchain_v1、model-profiles。

[UML]
@startuml
left to right direction
package "libs/" as Libs {
  component "core\nlangchain-core\n350 Python files" as Core
  component "langchain_v1\nlangchain\n123 Python files" as Active
  component "langchain\nlangchain-classic\n1581 Python files" as Classic
  component "partners\n15 integration packages\n363 Python files" as Partners
  component "standard-tests\nlangchain-tests\n36 Python files" as StandardTests
  component "text-splitters\n23 Python files" as Splitters
  component "model-profiles\nlangchain-profiles CLI\n7 Python files" as Profiles
}
component "langgraph" as LangGraph
component "Provider SDKs\nopenai, anthropic, ollama, qdrant" as ProviderSDKs
Active --> Core : imports abstractions
Active --> LangGraph : compiles agent StateGraph
Classic --> Core : legacy APIs
Classic --> Splitters : document utilities
Partners --> Core : implement interfaces
Partners --> StandardTests : inherit suites
Partners --> ProviderSDKs : call services
Profiles --> Partners : generates profiles
StandardTests --> Core : tests contracts
@enduml

[Detail]
从 `libs/` 开始读图：箭头表示代码依赖或生成关系。`langchain_v1` 是当前 `langchain` 包，它通过 `langchain-core` 和 `langgraph` 组合 agent 能力；`langchain-classic` 保留大量旧链、retriever、tool 和动态 deprecated import；partners 对外依赖 provider SDK，但对内应尽量只依赖 `langchain-core` 和标准测试。维护意义是：核心抽象改变会影响所有下游包，provider 包能力变化应通过 `langchain-tests` 和 profile 数据收敛。

------------------------------------------------------------
[Example]
包元数据和 CI 结构证据

[Description]
关键 `pyproject.toml` 证据：`libs/core` 的项目名是 `langchain-core`，版本 `1.4.0`；`libs/langchain_v1` 的项目名是 `langchain`，版本 `1.3.2`，依赖 `langchain-core`、`langgraph`、`pydantic`；`libs/langchain` 的项目名是 `langchain-classic`，版本 `1.0.7`；`libs/partners/openai` 的项目名是 `langchain-openai`，版本 `1.2.2`，依赖 `openai>=2.26.0,<3.0.0` 和 `tiktoken`；`libs/standard-tests` 的项目名是 `langchain-tests`，版本 `1.1.9`。

[UML]
None

[Detail]
CI 侧，`.github/workflows/check_diffs.yml` 按 changed files 选择 lint/test/integration jobs；`.github/workflows/_release.yml` 通过 `working-directory` 和 `release-version` 发布独立包；`.github/workflows/integration_tests.yml` 根据 working-directory 和 Python 版本构造集成测试矩阵；`pr_lint.yml` 强制 Conventional Commit scope。AGENTS 还要求 GitHub Actions `uses:` 固定到完整 commit SHA，实际 workflow 中能看到 `actions/checkout@de0fac2e...` 等 SHA pin。
