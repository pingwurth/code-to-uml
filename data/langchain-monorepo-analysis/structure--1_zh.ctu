Title: 顶层结构
Describe: S02_TOP_LEVEL_STRUCTURE。说明包级目录、核心配置、测试布局、导入边界和初始化副作用。
------------------------------------------------------------
[Example]
发布包与目录布局

[Description]
`libs/` 下每个主要目录都有自己的 `pyproject.toml`，形成独立版本和独立测试配置。当前发现的发布包包括 core、langchain v1、classic、text splitters、standard tests、model profiles 和多个 partner 包。

[UML]
@startuml
left to right direction
folder "libs" as Libs {
  folder "core" as Core
  folder "langchain_v1" as LangchainV1
  folder "langchain" as Classic
  folder "partners" as Partners
  folder "text-splitters" as TextSplitters
  folder "standard-tests" as StandardTests
  folder "model-profiles" as ModelProfiles
}
Core --> StandardTests : tested by contract suites
LangchainV1 --> Core : depends on langchain-core
Classic --> Core : depends on langchain-core
Classic --> TextSplitters : depends on langchain-text-splitters
Partners --> Core : provider implementations
Partners --> StandardTests : reuse shared tests
ModelProfiles --> Partners : writes data files
@enduml

[Detail]
图中的目录都来自 `find libs -maxdepth 3 -name pyproject.toml`。箭头按包配置和源码导入推断：例如 `libs/langchain_v1/pyproject.toml` 声明依赖 `langchain-core>=1.4.0,<2.0.0` 和 `langgraph>=1.2.3,<1.3.0`；`libs/partners/openai/pyproject.toml` 依赖 `langchain-core`、`openai` 和 `tiktoken`。这个结构允许 partner 包独立发布，但也要求 Core 的公共接口保持兼容。

------------------------------------------------------------
[Example]
核心源码目录

[Description]
`langchain_core` 按抽象类型组织，`langchain` v1 重点放在 agents、chat_models、embeddings、messages、tools 等薄入口，classic 保留 chains、agents、retrievers、utilities 等历史 API。

[UML]
None

[Detail]
关键目录包括 `libs/core/langchain_core/runnables`、`language_models`、`messages`、`tools`、`retrievers`、`vectorstores`、`documents`、`callbacks`、`tracers` 和 `_security`。v1 包位于 `libs/langchain_v1/langchain`，其中 `agents/factory.py` 定义 `create_agent`。classic 包位于 `libs/langchain/langchain_classic`，包含大量历史模块如 `chains`、`agents`、`utilities`、`vectorstores`。这种分层意味着新功能应优先放在 v1 或 partner 包，不能把新功能随意加回 classic。

------------------------------------------------------------
[Example]
测试与工具配置

[Description]
每个包的 `pyproject.toml` 都定义自己的 `test`、`lint`、`typing` 等 dependency groups，并通过 `tool.uv.sources` 指向同仓库 editable 包。

[UML]
None

[Detail]
`libs/core/pyproject.toml` 使用严格 mypy、ruff 全量规则和 `langchain-tests` 本地 source；`libs/langchain_v1/pyproject.toml` 将 `langchain-core`、`langchain-tests`、`langchain-text-splitters`、`langchain-openai`、`langchain-anthropic` 指向本地 editable 路径；partner 包如 OpenAI 使用 `langchain-tests` 契约测试。维护时应在目标包目录执行对应 `uv run --group test pytest ...`，而不是假设根目录存在统一 test runner。

