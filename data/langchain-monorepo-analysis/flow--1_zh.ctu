Title: 核心流程图
Describe: S05_CORE_FLOW。描述应用构建 agent、调用模型、执行工具、回写消息和处理结构化输出的主要路径。
------------------------------------------------------------
[Example]
v1 agent 执行主流程

[Description]
`create_agent` 组合 LangGraph 状态图后，运行时围绕 messages、model call、tool call 和 middleware hook 循环推进。

[UML]
@startuml
start
:用户调用 create_agent;
:解析模型、工具、middleware 和 response_format;
:构建 StateGraph;
:进入模型节点;
if (middleware before_model 或 wrap_model_call?) then (是)
  :执行 middleware hook;
endif
:BaseChatModel 生成 AIMessage;
if (AIMessage 包含 tool_calls?) then (是)
  :ToolNode 执行 BaseTool;
  :追加 ToolMessage 到 state.messages;
  :回到模型节点;
else (否)
  if (需要结构化输出?) then (是)
    :校验 structured_response;
  endif
  :返回最终 state;
endif
stop
@enduml

[Detail]
从 `create_agent` 开始读。关键节点分别来自 `libs/langchain_v1/langchain/agents/factory.py:697`、LangGraph `StateGraph`、Core `BaseChatModel` 和 `BaseTool`。箭头表示运行时状态推进。重要分支是工具调用循环和结构化输出校验；如果 middleware 动态加入未注册工具，会触发 `DYNAMIC_TOOL_ERROR_TEMPLATE` 对应的错误路径。

------------------------------------------------------------
[Example]
模型调用流程

[Description]
Core 的 `BaseChatModel` 负责把 message 输入规范化、进入 callback/tracing、执行 provider 实现、合并流式 chunk，并输出 `ChatResult` 或 `AIMessage`。

[UML]
@startuml
participant "Runnable.invoke" as Runnable
participant "BaseChatModel" as BaseChatModel
participant "CallbackManager" as CallbackManager
participant "Provider 实现" as Provider
participant "LangSmith / Tracer" as Tracer

Runnable -> BaseChatModel: messages + config
BaseChatModel -> CallbackManager: on_chat_model_start
CallbackManager -> Tracer: 记录输入和元数据
BaseChatModel -> Provider: _generate 或 _stream
Provider --> BaseChatModel: ChatGeneration 或 chunk
BaseChatModel -> CallbackManager: on_llm_end 或 on_llm_error
BaseChatModel --> Runnable: AIMessage / ChatResult
@enduml

[Detail]
顺序图强调“抽象层处理通用生命周期，provider 只实现具体请求”。`BaseChatModel` 位于 `libs/core/langchain_core/language_models/chat_models.py:270`，文件顶部导入 cache、callback、tracer、output parser、tool conversion 和 content block translator。错误路径中 `_generate_response_from_error` 会尝试从 provider 异常的 response、headers、status_code 和 request_id 中提取 metadata。

