Title: 核心对象/函数说明
Describe: S03_CORE_OBJECTS。列出主要类、函数和包级对象的责任、调用关系与维护边界。
------------------------------------------------------------
[Example]
核心抽象对象

[Description]
`langchain_core` 的核心抽象把 LLM 应用拆成 Runnable、模型、消息、工具、检索器、向量库和可序列化对象。

[UML]
@startuml
class "Runnable" as Runnable {
  invoke()
  ainvoke()
  batch()
  stream()
}
class "BaseLanguageModel" as BaseLanguageModel
class "BaseChatModel" as BaseChatModel
class "BaseTool" as BaseTool
class "BaseRetriever" as BaseRetriever
class "VectorStore" as VectorStore
class "BaseMessage" as BaseMessage
class "Document" as Document
class "Serializable" as Serializable

BaseChatModel --|> BaseLanguageModel
BaseLanguageModel --|> Runnable
BaseTool --|> Runnable
BaseRetriever --|> Runnable
BaseMessage --|> Serializable
Document --|> Serializable
@enduml

[Detail]
`Runnable` 位于 `libs/core/langchain_core/runnables/base.py:125`，提供 sync、async、batch、stream 和组合能力。`BaseChatModel` 位于 `libs/core/langchain_core/language_models/chat_models.py:270`，继承 `BaseLanguageModel` 并处理消息到 `AIMessage` 的模型调用。`BaseTool` 位于 `libs/core/langchain_core/tools/base.py:405`，把 Python callable 或结构化 schema 暴露为模型可调用工具。`BaseRetriever`、`VectorStore`、`BaseMessage`、`Document` 和 `Serializable` 分别承担检索、存储、消息、文档和序列化边界。

------------------------------------------------------------
[Example]
v1 agent 工厂

[Description]
`create_agent` 是 v1 包的高层入口，将模型、工具、middleware、结构化输出和 LangGraph 状态图组合成可执行 agent。

[UML]
@startuml
participant "用户代码" as User
participant "create_agent\nfactory.py:697" as Factory
participant "init_chat_model" as InitModel
participant "StateGraph" as Graph
participant "ToolNode" as ToolNode

User -> Factory: model, tools, middleware, response_format
Factory -> InitModel: 解析字符串模型或模型实例
Factory -> Graph: 构建 agent 状态图
Factory -> ToolNode: 注册工具执行节点
Factory --> User: CompiledStateGraph 或 Runnable
@enduml

[Detail]
顺序图回答“用户入口如何转成运行图”。`create_agent` 直接导入 `BaseChatModel`、`BaseTool`、LangGraph 的 `StateGraph`、`ToolNode`、`Command`、`Send`，并导入 middleware 和 structured output 类型。箭头表示创建阶段的职责移交。风险点是 middleware 可以改变 model/tool 调用路径，所以修改 factory 时要同时检查结构化输出错误处理、动态工具错误模板和 state schema。

------------------------------------------------------------
[Example]
Provider 实现对象

[Description]
Partner 包通过继承 Core 抽象提供具体 provider。OpenAI、Anthropic、Ollama、Groq、Mistral、DeepSeek、Perplexity 和 xAI 的 chat model 类都在 `libs/partners/*` 下实现。

[UML]
@startuml
class "BaseChatModel" as BaseChatModel
class "BaseChatOpenAI" as BaseChatOpenAI
class "ChatOpenAI" as ChatOpenAI
class "ChatAnthropic" as ChatAnthropic
class "ChatOllama" as ChatOllama
class "ChatGroq" as ChatGroq
class "ChatMistralAI" as ChatMistralAI
class "ChatDeepSeek" as ChatDeepSeek

BaseChatOpenAI --|> BaseChatModel
ChatOpenAI --|> BaseChatOpenAI
ChatDeepSeek --|> BaseChatOpenAI
ChatAnthropic --|> BaseChatModel
ChatOllama --|> BaseChatModel
ChatGroq --|> BaseChatModel
ChatMistralAI --|> BaseChatModel
@enduml

[Detail]
`ChatOpenAI` 位于 `libs/partners/openai/langchain_openai/chat_models/base.py:2534`。同类对象还包括 `ChatAnthropic`、`ChatOllama`、`ChatGroq`、`ChatMistralAI`、`ChatDeepSeek`、`ChatPerplexity` 和 `ChatXAI`。继承方向说明 provider 需要遵守 Core 的模型接口；其中 `ChatDeepSeek`、`ChatXAI` 继承 OpenAI 风格基类，表示它们复用 OpenAI 兼容协议的实现路径。

