Title: 核心原理、风险与改进建议
Describe: S09_CORE_PRINCIPLES 与 S11_RISKS_AND_IMPROVEMENTS。说明设计机制、权衡、主要风险和可执行改进。
------------------------------------------------------------
[Example]
核心设计原则

[Description]
工程的主要原则是：Core 抽象稳定、provider 实现隔离、用户 API 尽量组合 Core 对象、测试契约复用、模型能力数据集中生成。

[UML]
@startmindmap
* LangChain 设计原则
** 稳定接口
*** Runnable
*** BaseChatModel
*** BaseTool
** 实现隔离
*** partners/*
*** provider SDK
** 可组合运行
*** sync
*** async
*** stream
*** batch
** 契约测试
*** standard-tests
*** package pytest
** 数据维护
*** model-profiles
*** profile_augmentations.toml
@endmindmap

[Detail]
mindmap 从中心向外读。Core 抽象让用户代码和 provider SDK 解耦；Runnable 统一运行语义；standard tests 降低 partner 行为漂移；model profiles 把能力判断从硬编码挪到数据文件。权衡是包边界更多、版本范围更多、CI 配置更多，维护者必须按包级语义修改，而不能只看一个文件。

------------------------------------------------------------
[Example]
主要风险

[Description]
项目级风险集中在公共接口破坏、provider 行为偏移、profile 过期、classic 与 v1 双轨维护、测试入口分散和 CodeGraph 未初始化导致的结构分析缺口。

[UML]
None

[Detail]
1. 公共接口风险：`Runnable`、`BaseChatModel`、`BaseTool` 等 Core 方法签名变化会影响 v1、classic 和 partners。改动前应检查 `__init__.py` 导出、测试和示例。

2. Provider 边界风险：OpenAI 包明确只支持官方 OpenAI API；把第三方兼容字段放进 OpenAI 包会造成长期维护负担。

3. Profile 数据风险：模型能力数据生成后进入 partner 包，如果 refresh 不及时，structured output、tool calling 或 context window 判断可能不准确。

4. 双轨 API 风险：`langchain_v1/langchain` 和 `langchain/langchain_classic` 同时存在，新增功能放错层会扩大兼容成本。

5. 分散测试风险：没有根 `pyproject.toml`，命令需要在目标包下运行；误跑根目录命令可能得不到真实反馈。

------------------------------------------------------------
[Example]
改进建议

[Description]
这些建议不要求修改源码，是维护流程和报告复用层面的改进。

[UML]
None

[Detail]
优先级较高的改进：

- 初始化 CodeGraph：运行 `codegraph init -i` 后重新生成报告，可补齐 AST 级 callers/callees 和影响半径。
- 为常见维护任务补充包级命令索引：例如 core、v1、openai、text-splitters 的 test、lint、typing 命令，减少误跑。
- 对新增 partner 建立 checklist：同步 CI label、release workflow、issue template、dependabot、PR lint scope 和 standard-tests。
- 对 model profile refresh 增加最近刷新记录或生成来源说明，降低 profile 数据过期风险。

