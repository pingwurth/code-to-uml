Title: 关键代码片段解析
Describe: S08_CODE_SNIPPETS。聚焦短片段对应的设计意图、边界和维护注意事项。
------------------------------------------------------------
[Example]
Runnable 是组合单元

[Description]
`Runnable` 的文档明确把 invoke、batch、stream 和组合能力放在同一个抽象里，这是 Core 层最重要的设计支点。

[UML]
None

[Detail]
证据位于 `libs/core/langchain_core/runnables/base.py:125`。类文档说明 Runnable 是“can be invoked, batched, streamed, transformed and composed”的 unit of work，并强调 `RunnableSequence` 和 `RunnableParallel`。这解释了为什么模型、工具、检索器都倾向于继承或适配 Runnable：上层 chain 和 agent 可以不关心具体实现，只按统一运行接口组合。

------------------------------------------------------------
[Example]
BaseTool 的 schema 推断

[Description]
`BaseTool` 文件顶部包含 `_get_filtered_args`、`_parse_python_function_docstring` 和 `_infer_arg_descriptions` 等函数，用于把 Python 函数签名和 docstring 转成工具 schema。

[UML]
None

[Detail]
证据位于 `libs/core/langchain_core/tools/base.py`。`FILTERED_ARGS = ("run_manager", "callbacks")` 表明 tool schema 会过滤运行时注入参数；`_parse_python_function_docstring` 假设 Google Python style guide；`_validate_docstring_args_against_annotations` 会在 docstring 参数与类型注解不匹配时抛出 `ValueError`。因此新增 tool 时，签名、Annotated 描述和 docstring 必须同步，否则模型可见 schema 可能错误。

------------------------------------------------------------
[Example]
OpenAI 包的 provider 边界

[Description]
`ChatOpenAI` 文件开头有 API scope 警告：只面向官方 OpenAI API 规范，非标准 provider 字段不应在这里长期兼容。

[UML]
None

[Detail]
证据位于 `libs/partners/openai/langchain_openai/chat_models/base.py` 的模块 docstring。文件还维护 `_get_ssrf_safe_client`、`_MODEL_PROFILES`、message conversion、tool conversion、streaming 和 error metadata 提取等逻辑。维护者应把 provider-specific behavior 放到对应 partner 包，如 `langchain_deepseek` 或 `langchain_openrouter`，避免污染 OpenAI 官方语义。

